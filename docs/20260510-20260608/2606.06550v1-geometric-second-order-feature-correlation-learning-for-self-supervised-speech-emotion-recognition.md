---
title: Geometric Second-Order Feature Correlation Learning for Self-Supervised Speech Emotion Recognition
title_zh: 面向自监督语音情感识别的几何二阶特征相关性学习
authors: "Shuanglin Li, Ruxiao Qian, Siyang Song"
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.06550v1"
tags: ["query:affective-ai"]
score: 9.0
evidence: 利用二阶特征相关性的语音情感识别
tldr: 自监督学习在语音情感识别中能够提取丰富上下文特征，但传统一阶聚合假设特征独立，丢失了潜在黎曼几何与高阶关系。本文提出二阶相关层，将特征相关建模为协方差描述符，捕获协同共现模式，再通过对数欧氏映射投影到欧氏空间，保持几何完整性。实验表明该方法能恢复一阶池化损失的判别信息，有效聚合高维SSL特征，提升了语音情感识别性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.06550v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1399, \"height\": 553, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06550v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 770, \"height\": 570, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06550v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 806, \"height\": 341, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.06550v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1203, \"height\": 987, \"label\": \"Table\"}]"
motivation: 传统一阶聚合忽略特征间相关性及黎曼几何结构，限制了自监督语音情感特征的表征能力。
method: 提出二阶相关层，通过协方差描述符捕获特征协同模式，并利用对数欧氏映射从黎曼流形转换到欧氏空间进行学习。
result: 在ESD和RAVDESS数据集上，SOC层成功恢复一阶池化丢失的判别信息，有效聚合高维SSL特征，提升情感识别性能。
conclusion: 二阶相关层聚合自监督特征可恢复一阶池化丢失的判别信息，为语音情感识别提供了几何感知的特征聚合新思路。
---

## 摘要
自监督学习（SSL）为语音情感识别（SER）生成了强大且富含上下文的表示，但将这些表示聚合为整体描述符仍然是一个瓶颈。传统的一阶聚合隐含地假设特征独立，这忽略了潜在的黎曼几何，并丢弃了对于骨干网络表示能力至关重要的高阶关系。为了解决这一问题，本文提出了一种新颖的二阶相关（SOC）层。SOC不再孤立地处理特征，而是将特征相关性建模为协方差描述符，以捕获协同共现模式，这些模式可作为鲁棒情感识别的判别性签名。通过对数-欧几里得映射（LEM）将这些描述符从黎曼流形映射到欧几里得切空间，所提方法在保持几何完整性的同时，实现了直接的线性判别学习。在ESD和RAVDESS数据集上的大量实验表明，SOC恢复了一阶池化中丢失的判别信息，并有效地聚合了高维SSL特征。

## Abstract
Self-supervised learning (SSL) yields powerful, context-rich representations for speech emotion recognition (SER), yet aggregating these representations into holistic descriptors remains a bottleneck. Conventional first-order aggregation implicitly assumes feature independence, which overlooks the latent Riemannian geometry and discards higher-order relationships essential to the representational power of the backbone. To address this problem, this paper proposes a novel Second-Order Correlation (SOC) layer. Instead of treating features in isolation, SOC models feature correlations as covariance descriptors to capture synergistic co-occurrence patterns, which serve as discriminative signatures for robust emotion recognition. By mapping these descriptors from the Riemannian manifold to a Euclidean tangent space through Log-Euclidean mapping (LEM), the proposed method preserves geometric integrity while enabling direct linear discriminative learning. Extensive experiments on the ESD and RAVDESS datasets demonstrate that SOC recovers discriminative information lost in first-order pooling and effectively aggregates high-dimensional SSL features.

---

## 论文详细总结（自动生成）

### 论文核心问题与整体含义
- **核心问题**：自监督学习（SSL）虽能提取富含上下文信息的语音情感特征，但传统的一阶聚合（如平均池化、注意力池化）隐含假设特征相互独立，忽略了潜在的高阶统计依赖关系与黎曼流形结构，导致表征能力受限。
- **整体含义**：本文指出语音情感常由声学、韵律特征的协同作用体现，这种“二阶相关性”对情感判别至关重要，却被一阶方法丢弃。因此，需要一种能在保持几何结构的前提下，高效、稳定地建模二阶相关性的聚合方法。

### 方法论
- **核心思想**：提出二阶相关层（Second-Order Correlation, SOC），将 SSL 特征之间的共变关系建模为对称正定（SPD）流形上的协方差描述符，再通过对数-欧几里得映射（LEM）将其投影到欧氏切空间中，使其与下游线性分类器兼容，从而保留并利用高阶相关判别信息。
- **关键技术细节与流程**：
    1. **子空间投影与中心化**：对高维 SSL 特征 \( X \in R^{T \times D_{in}} \) 做全局时序均值中心化，再通过可学习线性映射 \( W \) 将其投影到低维空间 \( Z \in R^{T \times d} \)（\( d \ll D_{in} \)），以降低计算量与增强数值稳定性。
    2. **协方差计算与归一化**：计算样本协方差矩阵 \( C = \frac{1}{T-1} Z^\top Z \)，再对其进行迹归一化得到 \( \hat{C} \)，以消除非情绪性能量幅度影响，使其驻留在 SPD 流形上。
    3. **切线空间映射**：对 \( \hat{C} \) 进行特征分解，计算对数特征值矩阵 \( L \)，通过 \( S = U L U^\top \) 将协方差描述符从黎曼流形映射到欧氏切空间，实现非线性曲率到线性空间的转换。
    4. **半向量化**：利用对称矩阵的冗余性，通过 \( \text{vech} \) 算子提取下三角元素，得到紧凑的向量表示 \( v \in R^{d(d+1)/2} \)。
    5. 下游由 MLP 进行最终情感分类，整个框架可端到端优化。

### 实验设计
- **数据集与场景**：
    - **ESD**（中英双语，5种情绪，约35,000条语音）。
    - **RAVDESS**（英文，8种情绪，音频子集1,440条）。
- **测试基准与对比方法**：
    - 基准均采用 EmoBox 提供的说话人独立协议与 k 折交叉验证（ESD 5折，RAVDESS 6折）。
    - 对比三种一阶聚合方法：**GAP**（全局平均池化）、**ASP**（附加通道方差）、**FA**（基于幅度注意力的非均匀加权）。
- **骨干网络**：冻结的 Wav2Vec 2.0、HuBERT、WavLM（base 版本）作为特征提取器。

### 资源与算力
- 文中明确说明使用 **NVIDIA RTX 4090** 进行训练。
- 训练 **100 epoch**，优化器为 AdamW，峰值学习率 \(1\times10^{-4}\)，使用线性学习率衰减与10%预热。
- 未明确提及具体总训练时长。

### 实验数量与充分性
- **实验组数**：总共包含约 **18组主实验**（2数据集 × 3骨干 × 3基线 + 2数据集 × 3骨干 × SOC变体）以及消融分析。
- **消融实验**：
    - 移除 LEM 的对比（表1），验证流形映射的必要性。
    - 子空间维度 \( d \) 的敏感性分析（图2），考察不同维度对性能的影响。
- **可视化分析**：利用 t-SNE 对 WavLM 聚合特征进行分布可视化（图3）。
- **公平性与充分性**：实验覆盖了多种主流 SSL 骨干与基线，采用标准说话人独立协议，消融分析系统性地验证了核心组件的贡献，实验设计较为客观、充分。

### 主要结论与发现
- 所提 SOC 层在所有骨干网络和数据集上**一致且显著地优于**一阶聚合基线，例如在 Wav2Vec 2.0 上，WA 在 ESD 上提升 4.68%，RAVDESS 上提升 4.42%。
- 移除 LEM 会导致性能持续下降，证明将协方差描述符从黎曼流形映射到切空间对于几何保真和线性可分性至关重要。
- 子空间维度存在最优值：维度太低会“相关饥饿”，太高则引发维度灾难、协方差奇异与噪声放大。
- SOC 能有效收缩类内距离、扩大类间边界，形成更具内聚力和判别性的特征分布。

### 优点
- **问题定位清晰**：精准指出传统一阶聚合忽略二阶关系与黎曼几何这一具理论深度的局限。
- **方法创新性强**：将 SSL 特征相关建模为 SPD 流形上的协方差描述符，并引入 LEM 完成流形到欧氏空间的几何保持转换，形成端到端可学习的二阶聚合模块。
- **模块易集成**：SOC 被设计为“即插即用”层，可轻松嵌入现有 SER 框架。
- **实验扎实**：在多骨干、多数据集上验证，并通过消融和可视化提供深入见解，代码开源。

### 不足与局限
- **数据集规模有限**：仅在两个相对较小、受控的数据集上验证，未在规模更大、噪声更强的“in-the-wild”数据集（如 IEMOCAP、MELD）上测试，泛化性有待考证。
- **跨语种与跨场景验证缺失**：ESD 数据集已包含中英双语，但实验报告中未详细分析 SOC 在跨语种或跨文化场景下的性能表现。
- **骨干网络版本单一**：仅使用 Base 版本的 SSL 模型，未探索 Large 版本或不同预训练策略下的效果。
- **优化过程未详述**：未分析 SOC 在训练中的收敛速度、内存占用与基线相比的具体开销。

（完）
