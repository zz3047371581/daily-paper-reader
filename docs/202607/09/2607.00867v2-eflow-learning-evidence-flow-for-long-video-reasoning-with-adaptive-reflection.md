---
title: "EFlow: Learning Evidence Flow for Long-Video Reasoning with Adaptive Reflection"
title_zh: EFlow：通过自适应反思学习证据流以进行长视频推理
authors: "Wenhao Zhang, Kuanwei Lin, Xuyi Yang, Wei Gao, Ge Li"
date: 2026-07-01
pdf: "https://arxiv.org/pdf/2607.00867v2"
tags: ["query:long-video"]
score: 9.0
evidence: 分离时间定位的证据优先长视频推理
tldr: 长视频推理中，工具增强框架因定位与推理耦合，易陷入过早语义承诺，导致证据偏差与错误循环。EFlow基于Qwen3-VL，将过程解耦为时序定位链与推理链，先检索证据后推理，并配有置信度感知反思，自适应复核证据充分性。通过构建专用轨迹并采用监督微调、强化学习及强化微调训练，EFlow在五个视频理解基准上持续提升长视频推理。为证据偏差问题提供有效方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.00867v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 785, \"height\": 487, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00867v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1587, \"height\": 847, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00867v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 798, \"height\": 375, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00867v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 655, \"height\": 489, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.00867v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1590, \"height\": 1139, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.00867v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 800, \"height\": 515, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.00867v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 796, \"height\": 259, \"label\": \"Table\"}]"
motivation: 现有工具增强视频框架将时序定位与答案推理交织，引发过早语义承诺，导致证据检索偏差与错误推理循环。
method: EFlow基于Qwen3-VL，分离时序定位思维链和推理思维链，在答案生成前定位证据，并引入置信度感知反思自适应复核。
result: 在五个视频理解基准上，经多阶段训练后，EFlow一致提升长视频推理性能。
conclusion: EFlow验证了证据优先架构和自适应反思机制能有效缓解过早语义承诺，为长视频推理提供了可靠方案。
---

## 摘要
长视频推理从根本上受限于模型获取和利用视觉证据的方式。现有的工具增强视频框架常常在单一轨迹中交织时间定位和答案推理，导致早期语义假说偏置于证据定位。我们将这种失败模式称为过早语义承诺，即偏置的定位检索出不完整的证据，而不完整的证据进一步强化了错误的推理。为了解决这一问题，我们提出了EFlow，一个基于Qwen3-VL构建的证据优先视频推理框架。EFlow通过用于时间定位的思维链和用于推理的思维链，明确地将时间定位和逻辑推理分离开来，使模型能够在答案推断之前检索相关证据。此外，EFlow引入了一种置信度感知的反思机制，当检索到的证据可能不充分时，重新评估整个视频。我们进一步构建了专门的轨迹数据集，并通过监督微调、强化学习和强化微调来训练EFlow。在五个视频理解基准上的大量实验表明，EFlow持续提升了长视频推理的性能。

## Abstract
Long-video reasoning is fundamentally constrained by how models acquire and utilize visual evidence. Existing tool-augmented video frameworks often interleave temporal grounding and answer reasoning within a single trajectory, causing early semantic hypotheses to bias evidence localization. We term this failure mode premature semantic commitment, where biased grounding retrieves incomplete evidence and incomplete evidence further reinforces incorrect reasoning. To address this issue, we propose EFlow, an evidence-first video reasoning framework built upon Qwen3-VL. EFlow explicitly separates temporal grounding and logical reasoning through CoT for Temporal Grounding and CoT for Reasoning, enabling the model to retrieve relevant evidence before answer inference. In addition, EFlow introduces a confidence-aware reflection mechanism that re-evaluates the full video when retrieved evidence is potentially insufficient. We further construct dedicated trajectory datasets and train EFlow through supervised fine-tuning, reinforcement learning, and reinforcement fine-tuning. Extensive experiments across five video understanding benchmarks demonstrate that EFlow consistently improves long-video reasoning performance.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义 (研究动机和背景)

-   **核心问题：过早语义承诺**
    -   论文诊断了当前工具增强型长视频推理框架的一个核心失败模式，称为 **“过早语义承诺”**。
    -   在之前的框架中，时间定位（如“在哪里看”）和答案推理（如“答案是什么”）被耦合在同一个模型生成轨迹中。这导致模型在检索到充分证据前就形成初步答案假说，该假说反过来偏置证据定位，导致检索出片面的、不完整的证据，而这些不完整证据又进一步强化了最初的错误推理，形成误差级联。
-   **整体含义**
    -   长视频推理不仅是模型容量问题，更是一个 **“过程”问题**。模型必须在作出语义承诺（给出答案）之前，先可靠地定位并获取相关视觉证据。论文旨在通过设计一种 **“证据优先”的推理流程**来解决此问题。

### 2. 论文提出的方法论 (核心思想、关键技术细节)

-   **核心思想：学习“证据流”**
    -   提出 **EFlow** 框架，将长视频推理构建为一个由三个显式阶段组成的“证据流”，明确分离证据获取与答案生成，防止语义偏置。
-   **关键技术细节：三阶推理流程**
    1.  **时间证据流：用于时间定位的思维链**
        -   **T-CoT** 阶段，模型仅基于全视频和问题，调用一个时间裁剪工具`Crop_video(s, e)`，定位一个高度相关的证据片段`V_clip`。此阶段**严格禁止**预测最终答案。
    2.  **扎根推理流：用于推理的思维链**
        -   **R-CoT** 阶段，模型基于裁剪出的`V_clip`、原始问题和T-CoT的轨迹，进行逻辑推理并生成最终答案`A`。
        -   **置信度计算**：在生成答案标签的解码步骤，计算所有候选符元（如A， B， C， D）的 softmax 概率。**置信度分数 `c`** 定义为最高概率与次高概率之间的**差值**，`c = P(ytop1) - P(ytop2)`。
    3.  **自适应反思流**
        -   这是一种无需训练的推理策略。设定一个动态阈值`τ`。若`c < τ`，表明检索到的证据可能不充分，则触发“重读”机制：将全视频`V_full`和先前的推理轨迹`R`一起重新输入模型，进行全局再评估，生成最终答案`A_final`。

-   **训练数据与策略**
    -   **数据集构建**：
        -   **EFlow-SFT-50K**：基于Video-R1-CoT-165K，用Gemini-3-Flash生成带裁剪边界和T-CoT/R-CoT格式的轨迹，经答案匹配过滤和人工审核得到约5万条多轮轨迹，用于教会模型如何分阶段推理。
        -   **EFlow-RL-10K**：从VideoITG-40K数据集中抽取1万条数据，每条包含问题、正确答案和真实相关时间区间`[s_gt, e_gt]`，用于强化学习提供奖励信号。
        -   **EFlow-RFT-10K**：从强化学习阶段的高奖励轨迹中筛选，经人工审查后得到1万条高质量轨迹。
    -   **三阶训练策略**：
        1.  **监督微调**：在EFlow-SFT-50K上使用标准交叉熵损失训练，初始化模型的工具调用和分阶段推理能力。
        2.  **强化学习**：在EFlow-RL-10K上使用**GRPO算法**进行优化。奖励函数由三部分组成：
            -   **格式奖励 `r_format`**：正确遵循T-CoT/R-CoT XML标签格式得1.0，否则0.0。
            -   **时间定位奖励 `r_iou`**：计算预测区间与真实区间的**时间交并比**。
            -   **答案准确率奖励 `r_ans`**：由大语言模型DeepSeek-V3评判预测答案与真实答案的语义等价性，得分0.0到1.0。
            -   总奖励 `R = r_format + r_iou + r_ans`。
        3.  **强化微调**：在EFlow-RFT-10K上进行监督微调，将强化学习阶段的成功探索模式蒸馏回模型参数，以稳定和巩固已学到的行为。

### 3. 实验设计 (数据集、基准、对比方法)

-   **评估数据集与基准**：实验覆盖了五个视频理解基准，以评估不同视频长度的性能。
    -   **长视频基准**：**VideoMME**、**LVBench**、**LongVideoBench**。
    -   **通用/其他基准**：**NextGQA**、**VSI-Bench**、**Video-MMMU**。
-   **对比方法**：
    -   **开源单轮视频多模态大模型**：Qwen2.5-VL， LLaVA-Video， LLaVA-OneVision， Video-R1， Qwen3-VL等。
    -   **开源智能体框架**：VideoAgent， VideoTree。
    -   **开源原生多轮工具调用视频多模态大模型**：Conan， LongVT， Video-Zoomer， VITAL。
-   **基础模型与实现**：基于**Qwen3-VL-8B-Instruct**进行持续后训练。

### 4. 资源与算力 (GPU型号、数量、训练时长等)

-   **论文未明确提及**具体的GPU型号、显卡数量或总训练时长。
-   在强化学习阶段，指出了采用GRPO算法的**组大小`G=64`**，这暗示了计算资源需求，但未给出具体硬件配置。

### 5. 实验数量与充分性 (实验组数、充分性、公平性)

-   **实验组数较多且全面**：
    1.  **主对比实验**：在5个基准上与10余种前沿方法进行全面比较。
    2.  **消融实验**：
        -   **多阶段训练效果**：固定阈值下，对**SFT、RL、RFT**三阶段进行增量消融，评估各自贡献。
        -   **自适应反思效果**：将反思流与无反思的基线对比，并探究不同**置信度阈值`τ`**的影响。
    3.  **复杂推理实验**：在VSI-Bench和Video-MMMU上单独测试，证明了方法在复杂推理任务上的泛化性。
-   **充分性与公平性**：实验设计较为充分和公平。消融实验清晰揭示了各组件的贡献。所有对比均使用公开基准和方法，确保了可比性。

### 6. 论文的主要结论与发现

-   EFlow通过将时间定位与逻辑推理分离为T-CoT和R-CoT，有效缓解了长视频推理中的“过早语义承诺”问题，实现了显著的性能提升。
-   在长视频基准上，EFlow表现出明显优势，例如在VideoMME的“长”视频子集上达到60.1%，在LVBench上达到52.5%，均大幅超越同类工具增强框架。
-   基于竞争概率差的**自适应反思机制**是一种有效的“修复”路径，能在初始证据不足时通过全局重读来纠正错误，但必须保持选择性触发（适度阈值`τ`）以避免性能下降。
-   所提出的SFT→RL→RFT三阶段训练策略是有效的，RL探索出的最优策略通过RFT被成功稳定和巩固。

### 7. 优点 (方法或实验设计上的亮点)

-   **概念清晰，直击痛点**：准确诊断了“过早语义承诺”这一直觉上合理的关键问题，提出的“证据流”解法逻辑清晰、针对性强。
-   **推理与训练高度对齐**：T-CoT/R-CoT的推理分阶段设计，与专门为此构建的SFT、RL训练数据格式完全一致，确保了训练目标能有效转化为推理行为。
-   **训练策略完备**：SFT（格式初始化）→ RL（策略探索和优化）→ RFT（行为蒸馏和稳定）的三阶段流程系统性强，能充分利用不同类型的数据和优化方法。
-   **反思机制设计精巧且实用**：基于模型自身输出的竞争概率差来决定是否全局反思，无需额外训练，既利用了局部证据的精细度，又保留了从错误中恢复的全局视角，且通过动态阈值平衡了效果与效率。
-   **实验扎实全面**：在多个长视频基准和复杂推理基准上进行了验证，并通过细致的消融实验剖析了各组件的作用。

### 8. 不足与局限 (实验覆盖、偏差风险、应用限制等)

-   **基础模型依赖性强**：EFlow的性能依赖于基础模型能定位到合理的候选时间区域。论文仅在强大的Qwen3-VL上验证，若基础模型的时间定位能力较弱，EFlow框架可能因“垃圾进，垃圾出”而失效。缺乏在弱模型上的泛化性实验。
-   **自适应反思的校准风险**：反思机制的效用完全取决于置信度信号的校准质量。若模型输出的概率分布本身过于自信或过于犹疑，基于固定/动态阈值`τ`的触发策略可能失效。论文未讨论校准问题。
-   **资源需求未透明**：论文未公开训练所需的算力（GPU型号和数量），给复现其训练流程带来了不确定性。
-   **应用范围探讨有限**：当前的置信度评估和反思主要针对多选题场景设计。虽然文中提及可通过平均概率或困惑度扩展到开放式生成，但未进行相应实验验证。

（完）
