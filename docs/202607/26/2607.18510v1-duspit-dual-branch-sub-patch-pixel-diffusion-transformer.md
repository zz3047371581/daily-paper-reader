---
title: "DuSPiT: Dual-Branch Sub-Patch Pixel Diffusion Transformer"
title_zh: DuSPiT：双分支子块像素扩散Transformer
authors: "Yunpeng Bai, Yossi Gandelsman, Michaël Gharbi"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.18510v1"
tags: ["query:affective-ai"]
score: 9.0
evidence: 双分支像素空间图像生成Transformer
tldr: 现有像素空间扩散模型将图像块映射为单token，难以同时处理全局结构推理与细粒度细节。DuSPiT提出双分支子块像素Transformer，将紧凑的基础分支用于高效全局推理，并行的像素分支以子块组形式保留局部外观，两分支通过交叉注意力交互。该方法生成的图像细节更丰富、细粒度结构更强，同时实现了比先前像素扩散Transformer更优的质量与效率平衡。这一架构为像素空间扩散模型提供了有效分离全局与细节的新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.18510v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1457, \"height\": 700, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18510v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 574, \"height\": 431, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18510v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1363, \"height\": 847, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.18510v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 768, \"height\": 823, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18510v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 963, \"height\": 293, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18510v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1488, \"height\": 241, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18510v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1002, \"height\": 366, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18510v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1203, \"height\": 238, \"label\": \"Table\"}]"
motivation: 现有像素扩散模型使用单token表示图像块，迫使同一表示兼顾全局通信与局部细节，制约生成质量。
method: 设计双分支架构：基础分支进行紧凑全局推理，像素分支将子块分组保留细节，并通过交叉注意力实现两分支交互。
result: DuSPiT生成图像细节更丰富、细粒度结构更强，质量与效率权衡显著优于先前像素扩散Transformer。
conclusion: 通过双分支分离全局推理与局部建模，DuSPiT有效突破了像素扩散模型的细节生成瓶颈，提升了质量与效率。
---

## 摘要
扩散Transformer在图像生成任务中表现出色，但多数工作在压缩的潜在空间。像素空间扩散避免了这种信息损失，然而现有方法将每个原始图像块映射为单一令牌，迫使一种表示同时处理全局通信和细粒度细节。我们通过提出一种新架构DuSPiT来解决这个问题，它即双分支子块像素Transformer。该模型将全局结构推理与局部外观建模分离。DuSPiT使用紧凑的基本分支进行高效的全局推理，并设有一个并行的、高容量的像素分支，该分支被组织为子块组，以保留细节外观，两个分支通过交叉注意力进行交互。实验结果表明，DuSPiT生成的图像具有更丰富的细节和更强的细粒度结构，同时相比之前的像素空间扩散Transformer实现了更好的质量-效率权衡。

## Abstract
Diffusion Transformers achieve strong image generation performance, but most operate in compressed latent spaces. Pixel-space diffusion avoids this information loss, yet existing approaches map each raw image patch to a single token, forcing one representation to handle both global communication and fine-grained details. We address this issue by proposing a new architecture, \textbf{DuSPiT}, a \textbf{Du}al-branch \textbf{S}ub\textbf{P}atch \textbf{Pi}xel \textbf{T}ransformer. This model separates global structural reasoning from local appearance modeling. DuSPiT uses a compact base branch for efficient global reasoning and a parallel, high-capacity pixel branch, organized into subpatch groups, to preserve detailed appearance, with the two branches interacting through cross-attention. Our results show that DuSPiT generates images with richer details and stronger fine-grained structures, while also achieving a better quality--efficiency trade-off than prior pixel-space diffusion transformers.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：当前主流扩散 Transformer (DiT) 大多在压缩的潜在空间工作，虽然效率高，但自动编码器的压缩会丢失高频纹理、锐利边界和文字等细粒度视觉信号。像素空间扩散可避免此信息损失，但现有方法（如 JiT、PixelDiT）将每个原始图像块映射为**单一令牌**，迫使该表示同时承担**全局（块间）长程交互**与**局部（块内）细粒度外观建模**的双重任务，导致表示质量受限，单纯增加模型宽度又带来巨大的参数与计算开销。
- **整体含义**：论文指出像素空间扩散的核心挑战并非模型宽度不足，而是缺乏将高效的全局推理与高保真的局部细节建模进行**架构级分离**。为此提出 DuSPiT，用双分支设计解耦这两种功能，从而在维持效率的同时显著提升生成细节与结构保真度。

### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：双分支子块像素 Transformer（DuSPiT），为每个图像块维护两个耦合表示：
    - **基础分支（Patch Branch）**：紧凑的低维令牌 `zi ∈ R^{dp}`，负责通过标准 DiT 块进行高效的全局结构推理。
    - **像素分支（Pixel Branch）**：高容量的高维令牌 `ai ∈ R^{da}`（`da ≥ P²C`，远大于 `dp`），保留原始像素级的丰富外观信息。
- **关键技术细节**：
    - **子块分组与组织**：像素分支的高维令牌并非单一向量，而是被划分为 `n×n` 个子块组（sub-patch groups），每个组对应块内局部区域，并按位置赋予相对偏移量，从而保留原始像素内部的 2D 空间组织。
    - **层次化位置编码**：为对齐两个分支，采用融合位置方案——基础分支使用标准 2D RoPE 编码块中心坐标；像素分支中每个子块组的位置编码由**块级 RoPE + 相对子块偏移**得到，使模型同时感知块间布局和块内几何结构。
    - **非对称交叉注意力交互**：在每个 transformer 块中，基础分支先经过自注意力与 MLP，得到全局上下文令牌 `z'_i`；随后，像素分支的子块组作为**查询（Query）**，仅与**同一块**的基础分支令牌作为**键（Key）和值（Value）**进行交叉注意力，而基础分支不接收像素分支的信息。这种非对称设计使基础分支保持稳定的结构瓶颈，像素分支吸收全局语义来精炼外观。
    - **输出与预测**：最终去噪预测仅从像素分支产生，通过一个轻量的线性输出头得到重构图像 `x̂0`，基础分支不参与最终解码。模型采用 `x0` 预测参数化。
- **公式概览**：嵌入 `zi = Epatch(x_i_t), ai = Epixel(x_i_t)`；像素分支划分为子块组 `ai = [a_i^{(1)}, ..., a_i^{(n²)}]`；交叉注意力中 `q_i^{(r)} = MLP_q(a_i^{(r)})`，应用 RoPE 后得到旋转查询和键，仅本块内计算注意力并聚合值；输出更新后的像素表示。

### 3. 实验设计
- **数据集与场景**：所有生成模型在 **ImageNet 512×512** 分辨率下训练与评估，补丁大小固定为 32×32。
- **Benchmark 与对比方法**：广泛比较了**潜在空间扩散**模型与**像素空间扩散**模型。
    - **潜在空间**：DiT-XL/2、SiT-XL/2、REPA、DDT-XL/2、RAE 等。
    - **像素空间**：ADM-G、RIN、SiD(UViT/4)、VDM++、SiD2、PixNerd XL/16、**PixelDiT/32、DeCo/16、JiT-G/32** 等。其中 JiT 作为主要对比基线，其训练设置也被复用。
- **评价指标**：使用 **FID（↓）** 和 **IS（↑）** 衡量生成质量，同时记录单样本推理的 **GFLOPs** 来反映效率，并在质量-效率平面上进行可视化。
- **实现细节**：采用 Adam 优化器，学习率 2e-4，β1=0.9, β2=0.95，使用 x0 预测和 2.0 的噪声缩放，遵循 JiT 的配置以保证公平。

### 4. 资源与算力
- 论文中未明确给出训练时使用的 **GPU 型号、数量及具体训练时长**。仅提到训练设置与 JiT 保持一致，优化器和超参数如上述。对于模型规模，给出了参数量和 GFLOPs（例如 Ours-G/32 为 1.69B 参数、329 GFLOPs），并在局限部分指出高维像素分支增加了内存占用和反向传播成本，使较大变体需要更多 GPU 内存和更长的训练时间。

### 5. 实验数量与充分性
- **实验数量**：论文包含 **5 组主要表格**和 **3 幅对比/消融图**，覆盖了约 **15 种不同配置**（不同模型尺度、消融变体）。
    - **主实验（Table 1 & Figure 2）**：对比了 12+ 种现有像素及潜在空间模型，展示了质量‑效率权衡。
    - **规模对比（Table 2）**：在 B/L/H/G 四种参数规模下，与 JiT 进行一对一比较（8 个条目）。
    - **消融实验**：包括额外维度大小（4 种）、交叉注意力信息流（4 种）、投影与调制设计（两种子消融共 4 种）。
    - **定性分析（Figure 3）**：提供了与 JiT、DeCo 的生成图像对比。
- **充分性与公平性**：实验设计**充分且客观公平**。与同期最强的像素扩散方法（JiT、PixelDiT、DeCo）直接对标，且严格对齐 JiT 的训练超参数。消融研究覆盖了架构中的核心设计选择（维度、交互、投影），能清晰反映每个设计的作用。同时控制了参数量和 GFLOPs 进行对比，避免了因算力优势导致的误判。

### 6. 论文的主要结论与发现
- DuSPiT 通过将全局结构推理（基础分支）与局部外观建模（像素分支）**显式解耦**，有效解决了像素空间扩散 Transformer 中单一令牌的表示瓶颈。
- 在 ImageNet 512×512 上，DuSPiT 在所有模型尺度下均**显著优于 JiT**（如 G 规模下 FID 1.52 vs 1.78，且参数量和 FLOPs 更少），并实现了当时像素空间方法中最佳的 **FID‑GFLOPs 权衡**。
- 消融实验证实：非对称的交叉注意力（只从基础分支流入像素分支）、层次化位置编码、按子块区分的投影与调制，对保持高质量生成至关重要；额外像素维度的容量需适中，过大反而会导致优化困难。

### 7. 优点：方法或实验设计上的亮点
- **架构创新性强**：首次明确提出像素扩散中“全局通信”与“局部保留”应使用不同表示空间，并设计了优雅的双分支耦合机制，非对称交叉注意力设计简洁有效。
- **保留空间结构**：将像素分支组织为子块组并配合层次化位置编码，使模型能够维持原始像素的 2D 几何先验，而非简单的扁平高维向量。
- **实验极为扎实**：进行了系统性消融（维度、交互流、投影方式），并通过多尺度对比（B→G）和与十余种强基线的定量比较，充分证明了方法的有效性和效率优势，对后续研究有良好的参考价值。
- **分析深刻**：明确指出性能提升并非来自盲目增大模型，而是源自合理的架构分工，并可视化展示了细节保真度的定性改善。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等
- **训练成本较高**：高维像素分支增加了内存和反向传播开销，训练大模型需要更多资源，作者也直言不讳地指出这是当前的主要限制。
- **扩展性待验证**：实验仅基于 **ImageNet 512×512** 单个数据集，未测试更多样化的数据集（如文本到图像生成、更高分辨率或更复杂场景），其在开放域生成中的鲁棒性未知。
- **未探索应用**：论文未进行下游任务（如图像编辑、超分辨率、条件生成）的评估，目前止步于无条件/类别条件生成。
- **架构优化空间**：虽提出了子块分组，但所有子块仍与同一块的基础令牌做全连接注意力，若将交叉注意力也设计为局部或稀疏形式，可能进一步降低计算复杂度，目前未有探索。
- **未来工作**：作者也指出需研究更高效的训练策略、跨子块参数共享、自适应维度分配，以及将该设计扩展到视频生成等领域。

（完）
