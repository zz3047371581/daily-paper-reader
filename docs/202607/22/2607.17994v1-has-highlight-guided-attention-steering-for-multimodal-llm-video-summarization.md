---
title: "HAS: Highlight-guided Attention Steering for Multimodal LLM Video Summarization"
title_zh: HAS：基于亮点引导的注意力调控用于多模态大语言模型视频摘要
authors: "Rui Chu, Yingjie Lao"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.17994v1"
tags: ["query:long-video"]
score: 9.0
evidence: 高亮引导注意力导向的多模态LLM视频摘要
tldr: 视频摘要任务旨在高效导航与检索视频内容，但现有方法多依赖离散关键帧，忽视帧间连续性与全局重要性，导致理解不连贯和信息丢失。本文提出高亮引导注意力调控方法HAS，首先计算整段视频的连续帧级高亮分数，然后将高亮分布向量化，在推理时注入多模态大语言模型，调控其对不同帧的注意力权重，使得高亮帧获得更多关注，而低亮帧保留一定关注以避免遗忘。在多个视频摘要基准上，HAS展现出有说服力的性能，证明连续高亮引导有效提升了摘要的连贯性和完整性，尤其在全视频理解的场景下优势明显。该方法突破了离散选帧的局限，为视频摘要及连续视频理解任务提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.17994v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1267, \"height\": 605, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17994v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1256, \"height\": 428, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17994v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 539, \"height\": 293, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17994v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1258, \"height\": 1187, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.17994v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1254, \"height\": 261, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17994v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 765, \"height\": 1026, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17994v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1217, \"height\": 444, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17994v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1367, \"height\": 459, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17994v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 508, \"height\": 358, \"label\": \"Table\"}]"
motivation: 现有视频摘要方法仅选取离散关键帧并生成片段描述，忽视全局帧重要性，导致多模态大模型无法充分理解连续视频的完整上下文。
method: 提出HAS：先学习连续帧级高亮分布，再将其转化为注意力调控向量，在模型推理时动态调整对每帧的注意力权重，聚焦高亮帧且保留对低亮帧的关注。
result: 在多个基准上，HAS取得了有说服力的视频摘要性能，证明连续高亮引导能有效保留全局上下文并提升摘要质量。
conclusion: HAS通过注意力调控实现连续视频摘要，突破离散选帧局限，为多模态模型处理长视频提供了新见解，有望应用于更广泛的视频理解任务。
---

## 摘要
随着人工智能在视频生成领域的发展，视频理解变得愈加重要。近来，多模态大语言模型已展现出其在视频理解中的能力。作为视频理解的一个特定领域，视频摘要对于高效导航与检索的重要性已得到证实。视频理解与视频摘要均需对视频中的关键帧进行良好选取。当前视频摘要方法高度聚焦于所选关键帧及其相关联的片段描述，却忽视了从全局视角看待帧重要性的方式。我们认为，使用离散选择的帧进行摘要不仅会降低理解的连贯性，还会丢失视频中的重要信息，并浪费多模态大语言模型原有的处理能力。本文提出了一种称为亮点引导注意力调控的视频摘要方法（HAS）。我们考虑了一种具有挑战性但实用的设定，即提供给多模态大语言模型进行摘要的视频应为连续的，但需有亮点引导。HAS主要由两部分构成：第一部分是为整个视频寻找连续的帧级亮点分布；第二部分是将该亮点分布作为注意力调控向量作用于多模态大语言模型，旨在更好地理解视频，从而在模型推理期间将更多注意力集中于高亮帧，同时通过对低亮帧分配较少注意力而非遗忘它们，避免丢失全部信息。我们在多种基准上评估了HAS，其在视频摘要任务中表现出令人信服的性能。

## Abstract
Video understanding has become more and more important with the growth of Artificial Intelligence (AI) for video generation. Recently, Multimodal Large Language Model(M-LLM) has shown its capability in video understanding. Video summarization, a specific domain of video understanding, has proven its importance for efficient navigation and retrieval. Both video understanding and video summarization require a good selection of key frames in a video. Current video summarization methods heavily focus on the selected key frames and correlated segment captions. However, existing approaches overlook the perspective of treating the importance of the frames globally. We argue that using discrete selected frames for summarization will not only reduce the understanding coherence, but also lost important information in the video, as well as wasting the original capacity of the MLLMs. In this paper, we propose HAS, a Highlight-guided Attention Steering method for video summarization. We consider a challenging but practical setting where the video given to MLLMs for summarize should be continuous but with highlight guidance. HAS mainly consists of two parts: The first part is to find a continuous frame-level highlight distribution for the video globally. The second part is to apply the highlight distribution as an attention steering vector for the MLLM, targeting a better understanding of the video, and thus during the model inference time, putting more attention on the highlighted frames, while avoiding lost entire information on less highlighted frames through putting less attention instead of forgetting them. We evaluated HAS on a variety of benchmarks, and it has shown convincing performance in video summarization.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义  
- 视频摘要需从完整视频中选取关键帧并生成简明总结，但现有方法普遍采用“先离散选帧，再基于所选子集摘要”的流水线。  
- 该范式导致两个问题：  
  - 硬性丢弃低分帧会丢失重要背景和细节，破坏理解的连贯性。  
  - 削弱多模态大语言模型本身通过注意力机制进行全局建模的能力，造成模型容量浪费。  
- 论文类比人类观看视频时记忆侧重高光时刻、同时保留整体印象的过程，提出**连续高亮引导的注意力调控（HAS）** 框架，使模型在推理过程中对不同帧分配差异化的注意力，而非硬性剪裁。

## 2. 提出的方法论  
HAS 包含两个阶段，全部在推理时实施，无需训练或微调基础模型。  

### 2.1 连续高亮分布构建  
- 使用已有的高亮/时刻检测模块（如 QVHighlights）获得原始时序分数 \(\hat{\mathbf{h}}\)。  
- 将分数插值到目标帧数 \(T\)，再做最小‑最大归一化，得到连续、有界的分布 \(\mathbf{h}\in[0,1]^T\)（算法 1）。  
- 通过对分数做时间平滑，消除噪声并保持时序连续性。

### 2.2 从高亮分布到调控向量  
- 考虑 MLLM 对每帧提取 \(P\) 个视觉 token，将帧级分数 \(\mathbf{h}_t\) 复制 \(P\) 次，得到 token‑级的重复向量。  
- 对该向量取对数并加上小常数 \(\epsilon\) 以防止低分帧的注意力完全消失，形成调控向量 \(\mathbf{V} = \log(\text{Repeat}(\mathbf{h},P)+\epsilon) \in \mathbb{R}^{TP}\)（公式 2）。

### 2.3 推理时注意力调控  
- 选定一小部分视觉交叉注意力头（记为集合 \(\mathcal{S}\)）。  
- 对每个被选中的头 \((\ell,m)\)，在其预 softmax 注意力 logits \(\mathbf{A}^{(\ell,m)}\) 上添加逐行偏置：  
  \[
  \tilde{\mathbf{A}}^{(\ell,m)}_{i,:} = \mathbf{A}^{(\ell,m)}_{i,:} + g_{\ell,m}\,\beta_{\ell,m}\,\mathbf{V}
  \]  
  其中 \(g_{\ell,m}=\sigma(a_{\ell,m})\in[0,1]\) 为可学习的门控系数，\(\beta_{\ell,m}\) 为调控强度（公式 3，4）。  
- 通过最小化参考摘要的负对数似然并加上鼓励稀疏使用头数的正则项 \(\lambda_s\sum g_{\ell,m}\)，仅优化参数 \(\Theta=\{a,\beta\}\)，而整个 MLLM 和亮点生成器保持冻结。优化在验证集上完成，测试时直接使用固定参数进行前向调控（算法 2）。  
- 对数空间的 \(\mathbf{V}\) 实际上实现的是乘法注意力重加权，同时保持了数值稳定性和与掩码机制的兼容。

## 3. 实验设计  
### 数据集与基准  
- **V2V 摘要**：SumMe、TVSum，指标 Kendall’s τ 和 Spearman’s ρ。  
- **跨模态摘要**：VideoXum，评估 V2T（BLEU‑4/METEOR/ROUGE‑L/CIDEr）、V2V（F1/Spearman/Kendall）和 V2VT（FCLIP/Cross‑FCLIP）。  
- **科学视频摘要**：VISTA，评估 ROUGE‑Lsum、BERTScore，以及视频对齐度VideoScore 和事实一致性 FactVC。  
- **零样本迁移**：从 SumMe 直接测试到 MR.HiSum。

### 对比方法  
- 纯视觉方法：VASNet、DSNet、DMASum、PGL‑SUM 等。  
- 视觉+文本方法：CLIP‑It、A2Summ、Argaw et al. 等。  
- LLM 中心方法：零样本 LLM、LLMVS、V2Xum‑LLaMA。  
- 强大的闭源模型（GPT‑o1、Gemini 2.0）作为参考。

### 基础模型  
- 在多种开源视频 MLLM 上验证，包括 mPLUG‑Owl3、LLaVA‑NeXT‑Interleave、Video‑LLaVA、LLaMA‑VID、Video‑ChatGPT 和 Video‑LLaMA。

## 4. 资源与算力  
- 论文中仅说明“所有实验均在 Nvidia L40S GPU 上运行”，**未提供 GPU 数量、训练时长或推理时间开销等具体算力信息**。

## 5. 实验数量与充分性  
- 总计覆盖至少 4 个基准（SumMe/TVSum、VideoXum、VISTA、MR.HiSum），涉及 V2V、V2T、V2VT 等多种任务。  
- 在 VISTA 上分析了覆盖率随视频预算的变化曲线（图 2），并提供了不同 MLLM 骨干上的一致性增益（图 3），显示出方法不依赖特定架构。  
- 可视化展示了高亮曲线与真实标注的一致性，以及校准→调控的信号流（图 4）。  
- 虽未见显式消融实验（如调控头数量、正则参数的影响），但通过多基线对比、零样本迁移和骨干一致性分析，实验设计客观、公平，且覆盖了代表性的评估维度。

## 6. 主要结论与发现  
- 连续注意力调控能够可靠地提升视频摘要的时序重要性排序与人类标注的一致性（表 1）。  
- 在不牺牲文本质量的前提下，增强了跨模态摘要的时序显著性和视频‑文本语义对齐（表 2）。  
- 在长视频科学报告摘要中，显著改善了事实一致性和证据利用率，且随帧数增多其信息覆盖衰退更慢（表 3，图 2）。  
- 方法具备良好零样本泛化能力，可直接迁移到新数据集而不必重新训练（表 4）。  
- 增益在多种 MLLM 上普遍存在，说明注意力调控是一种模型无关的轻量增强手段（图 3）。

## 7. 优点  
- **创新视角**：首次将推理时注意力操控引入视频摘要，将离散选帧思路转变为连续注意力偏置。  
- **保留完整信息**：避免硬性丢弃帧，有助于保持摘要的连贯性和完整性，充分利用模型上下文容量。  
- **即插即用**：无需重新训练或微调骨干模型，仅需在验证集上学少数参数，易于集成到现有视频 MLLM 中。  
- **实验扎实**：覆盖多任务、多数据集、多骨干，对比了众多强基线，且有机制可视化支撑。  
- **性能提升一致**：在不同设置下均观察到稳定的性能增益，且对事实一致性的提升在多种模型上成立。

## 8. 不足与局限  
- **依赖高亮先验质量**：调控效果受制于现成的亮点检测模块，若先验不准确或噪声大，可能误导注意力偏置。  
- **平滑假设的局限**：当前校准以平滑曲线为目标，可能难以应对需要精确时间边界的短高频事件或突变切换。  
- **调控参数调优需求**：尽管只学少量参数，仍需要一小批带参考摘要的验证数据来调节门控和强度，限制了完全零资源部署。  
- **实验细节缺失**：未给出推理速度、额外显存占用等成本指标，也未讨论对过长视频（数千帧）的扩展方案。  
- **偏置风险**：查询引导的亮点分布可能受用户提示偏见影响，生成有偏向的摘要，论文对此虽有提及但未深入分析缓解措施。  
- **方法深度有限**：当前仅干预视觉交叉注意力头，未探索其他层的调控或与参数高效微调的结合可能。

（完）
