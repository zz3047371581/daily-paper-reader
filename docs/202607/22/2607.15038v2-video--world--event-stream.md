---
title: Video = World + Event Stream
title_zh: 视频 = 世界 + 事件流
authors: "Lianghua Huang, Zhi-Fan Wu, Yupeng Shi, Wei Wang, Mengyang Feng, Cheng Yu, Chen Liang, Junjie He, Chen-Wei Xie, Yu Liu, Jingren Zhou, Ang Wang, Bang Zhang, Baole Ai, Chongyang Zhong, Jinwei Qi, Kai Zhu, Pandeng Li, Peng Zhang, Wenyuan Zhang, Xinhua Cheng, Yitong Huang, Yun Zheng, Yuxiang Bao, Yuzheng Wang, Zhiwei Lin, Zoubin Bi"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.15038v2"
tags: ["query:long-video"]
score: 9.0
evidence: 将视频建模为世界加事件流，用于实时事件理解
tldr: 现有视频不能有效建模持久世界与动态事件间的统一性。Wan-Streamer v0.3提出将视频解构为世界加事件流，世界包含场景、主体等稳定因素，事件流包含行为、语音等时变内容。基于此视角的预训练任务学习预测世界的实时响应，并将多模态输入映射为语音与行为动作。在实时视听交互中，模型实现640x368分辨率25FPS视频、160毫秒流式单元、约200毫秒模型侧延迟，总交互延迟约550毫秒。该框架可作为通用视频理解范式，广泛适配实时下游任务，推动流式视听智能发展。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.15038v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1721, \"height\": 673, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15038v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1720, \"height\": 776, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15038v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1709, \"height\": 686, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.15038v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1721, \"height\": 665, \"label\": \"Table\"}]"
motivation: 将视频分解为世界与事件流，以统一持久上下文与动态变化，支持实时流式预训练与交互。
method: 基于世界与事件流视角，设计预训练任务预测世界实时响应，模型以视觉-语言-行动式映射输出语音与行为。
result: 保持640x368分辨率25FPS视频与160毫秒流式处理，模型侧响应延迟约200毫秒，总交互延迟约550毫秒。
conclusion: 提出通用视频预训练框架，可高效适配全双工视听交互等多种实时下游任务，实现低延迟流式交互。
---

## 摘要
我们提出Wan-Streamer v0.3，它将我们的原生流式交互模型重新组织在一个统一的观点下：一个视频就是一个世界加上一个事件流。世界是指视频展开的持久上下文，包括环境、场景、主体、环境声条件、语音特征以及其他相对稳定的条件。事件流则是该世界内随时间变化的一切，包括场景或环境变化、主体行为、语音和其他声音。这产生了一个基于大量真实视频的通用预训练任务：给定一个世界和传入的输入，预测世界如何实时移动、变化和响应。由此获得的能力可以专门化到广泛的实时下游任务家族中。我们在实时全双工视听交互中实例化它，此时事件流是智能体的语音连同自由形式的行为。在功能上，模型的多模态理解过程类似于视觉-语言-动作：它将多模态用户输入映射为语言形式的语音和行为动作。Wan-Streamer v0.3保持了v0.2的运行指标：640×368分辨率、25 FPS视频，160毫秒流式单元，约200毫秒模型端响应延迟，在350毫秒双向网络预算下总交互延迟约550毫秒。

## Abstract
We present Wan-Streamer v0.3, which reframes our native-streaming interaction model under a single organizing view: a video is a world plus an event stream. The world is the persistent context in which a video unfolds, including the environment, scene, subjects, ambient acoustic conditions, voice characteristics, and other relatively stable conditions. The event stream is everything that changes over time within that world, including scene or environmental changes, subject behavior, speech, and other sounds. This yields a general-purpose pretraining task over large amounts of real video: given a world and incoming input, predict how the world moves, changes, and responds in real time. The resulting competence can be specialized to a broad family of real-time downstream tasks. We instantiate it on real-time full-duplex audio-visual interaction, where the event stream is the agent's speech together with free-form behavior. Functionally, the model's multimodal understanding process is vision-language-action-like: it maps multimodal user input to language-form speech and behavior actions. Wan-Streamer v0.3 preserves the v0.2 operating point: 640x368 video at 25 FPS, a 160 ms streaming unit, approximately 200 ms model-side response latency, and approximately 550 ms total interaction latency under a 350 ms bidirectional network budget.

---

## 论文详细总结（自动生成）

好的，以下是对论文《Video = World + Event Stream》的结构化中文总结。

### 1. 论文的核心问题与研究动机

*   **核心问题**：如何构建一个统一且可扩展的框架，使得实时交互模型不仅能像前代版本（v0.1/v0.2）那样进行语音对话，还能在理解持久场景上下文（世界）的基础上，实时地产生开放词汇、自由形式的行为（如肢体动作、表情、环境交互）。
*   **整体含义**：论文提出一个根本性的视角转换：将任何视频都解构为一个相对稳定的“世界”和一个随时间变化的“事件流”。这个视角不仅统一了对视频数据的理解，还定义了一个通用、可扩展的预训练任务，从而赋予智能体一种元能力——学会“世界如何随时间并响应输入而演变”。这一能力能被专门化部署到各类实时下游任务（如全双工视听交互）中。

### 2. 方法论

#### 核心思想：世界 + 事件流分解
*   **世界（World）**：定义为视频中持久不变的上下文，包括场景环境、布局、主体（身份、外貌）、环境声和语音特征等。形式上用一个灵活的、根据任务可变的架构化记录 \(W\) 表示。
*   **事件流（Event Stream）**：定义为在“世界”中随时间变化的一切，包括主体行为、语音、相机运动、环境变化等。每个事件是一个时域局部化的记录 \(e_k\)，包含活动区间、描述和关联角色。
*   **视频即世界 + 事件流**：一个视频就是事件流在特定世界中被观测到的因果实现。

#### 通用预训练任务
*   基于上述分解，定义了一个通用的流式预训练目标：**给定世界 \(W\) 和因果历史输入 \(x_{\le k}\)，自回归地预测下一个事件单元 \(e_k\) 及其视听实现**。
*   公式化为：\(p_\theta(e_{1:K} | W, x_{1:K}) = \prod_{k=1}^{K} p_\theta(e_k | W, x_{\le k}, e_{<k})\)，这与 v0.1 的因果框架一致，但将“世界”显式化为一级条件对象。
*   该任务可在大规模、多样化的真实视频上训练，学到的“世界事件动力学”知识可迁移至多个下游实时任务。

#### 下游实例化：实时全双工视听交互
*   **世界设置**：实例化为智能体的场景、角色（身份/外貌/人格）、环境音、语音音色。
*   **事件流驱动**：用户流式输入（文本、音频、视频）作为 \(x_{\le k}\) 驱动智能体的事件流。
*   **自由形式行为接口**：采用“角色扮演聊天”格式，智能体输出的文本流中，**用括号描述的行为指令与口语词交替出现**，例如：“(皱眉) 你说什么？”。这使智能体能够执行开放词汇、情境化行为，而不再局限于固定动作集。
*   **功能映射**：模型的理解过程是类似视觉-语言-动作（VLA）的映射，将多模态输入映射为语言形式的语音和行为。这些语言 token 共同条件化后续同步的视听生成（由 v0.1 因果解码器完成）。
*   **拓扑与指标保持**：建模契约和部署拓扑完整继承自 v0.2，保持了 `640×368@25FPS` 视频流、`160ms` 流式单元、约 `200ms` 模型端延迟和约 `550ms` 总交互延迟。

### 3. 实验设计

本文非传统实验对比论文，其实验部分主要聚焦于系统运行指标的验证与定性观察。
*   **数据集/场景**：未提及具体预训练数据集的名称或规模。仅说明预训练在“大规模、多样化的真实视频”上进行。下游应用场景为实时全双工视听对话。
*   **基准（Benchmark）与方法对比**：**没有与任何外部方法（如其他交互模型）进行定量对比**。对比完全发生在自身版本迭代上（见下文第4点）。实验未使用任何定量基准测试或指标（如准确率、FID、用户评分等）。
*   **度量指标**：仅使用了两个系统性能指标：**模型端延迟（~200ms）**和**总交互延迟（~550ms）**，作为与 v0.2 保持一致的证据。

### 4. 资源与算力

*   **文中未明确说明**训练所使用的 GPU 型号、数量、训练时长、batch size 等算力细节。论文仅提及其**服务拓扑**“不变地继承自 v0.2”，v0.2 使用了“思考者（Thinker）+ 基于 Ulysses 上下文并行的表演者（Performer）”架构，但未透露具体硬件资源配置。

### 5. 实验数量与充分性

*   **实验数量极少**。严格来说，**没有进行传统意义上的性能比较实验或消融实验**。
*   **主要“实验”**是报告了两个系统延迟指标，以证明 v0.3 继承了 v0.2 的运行性能，并辅以一段**定性行为观察**描述（如“动作与语音同步”、“保持了稳定的空闲和聆听行为”）。
*   **缺乏充分性与客观性**：作为一篇研究论文，其实验部分极度不充分。它完全没有提供：
    *   自由形式行为的生成质量、准确率或多元性的量化评估。
    *   与世界上下文的一致性、行为是否合理等指标的评测方法或结果。
    *   与任何基线的比较（无论是系统延迟以外的性能，还是生成行为质量）。
    *   用户主观评价（MOS 等）。
    *   仅凭自身版本迭代的系统延迟指标和一段主观观察，**远远不足以客观、公平地证明其主张的“扩展能力”和“优越性”**。

### 6. 主要结论与发现

*   论文提出的“世界+事件流”分解为原生流式生成提供了一个有效的重构视角。
*   该视角支持一个通用的视频预训练任务，理论上能迁移到多种实时任务（本文仅展示了全双工视听交互）。
*   对于交互任务，通过引入角色扮演式的语言流，Wan-Streamer v0.3 成功将智能体的表达能力从 v0.2 的语音和隐式聆听，**扩展至开放词汇、自由形式的行为**。
*   这种行为能力的扩展**完整保留了 v0.2 的建模契约和低延迟运行指标**（`640×368@25FPS`，`200ms` 模型延迟，`550ms` 总延迟），未引入新的延迟关键路径。

### 7. 优点

*   **概念统一且优雅**：“世界+事件流”的抽象将持久上下文与动态变化解耦，这对通用视频理解和实时生成任务具有深刻的理论指导意义。
*   **通用性与可扩展性**：该框架不局限于单一交互应用，其预训练目标天然适合大规模视频学习，论文描绘了向世界模型、具身操控等任务迁移的蓝图。
*   **实用的接口设计**：采用自然语言中的括号表示行为，简单易用，且实现了开放词汇的动作控制，无需预先定义复杂的动作空间。
*   **系统工程稳健**：在增加新模态能力的同时，确保了延迟、分辨率等系统关键性能指标不退化，显示出扎实的工程继承能力。

### 8. 不足与局限

*   **实验严重匮乏，证据不足**：这是本篇论文最大的局限。全文无任何量化评估、基线对比或消融实验，所有结论几乎仅依靠系统延迟指标和定性叙述支撑，缺乏科学说服力。
*   **对比仅限于自身前代版本**：所有比较均建立在自身产品的版本迭代上（v0.1/v0.2），缺乏与领域内其他现有实时交互模型（如 GPT-4o、EVI 3 等）的多维度公平对比。
*   **定性观察主观性强**：对于核心贡献“自由形式行为”，其正确性、合理性、与世界的吻合度、与语音的同步效果等关键质量维度，完全依赖作者的主观观察描述。
*   **迁移潜力未验证**：论文声称其通用预训练能力可迁移至“世界模型控制”、“具身操控”等其他任务，但对此**未提供任何实验证据或初步结果**，仅停留在设想阶段。
*   **技术细节披露不足**：完全未提及预训练数据规模、构成、模型参数量、训练细节（算力、技巧）以及实现“世界”与“事件”自动标注的方法，可复现性极低。
*   **角色独立性未深入**：虽然声明支持多角色，但实验和示例均侧重于单智能体或者事件与角色简单关联，对于复杂多角色互动场景下的事件归属和同步问题未做探讨。

（完）
