---
title: "ChronoStitch: Training-Free Composition of Visual KV Memories for Long-Horizon Temporal Reasoning"
title_zh: ChronoStitch：长时域推理中视觉KV记忆的训练自由组合
authors: "Santiram Tiwari, Nishant Sinha, Kunal Kislay"
date: 2026-07-21
pdf: "https://arxiv.org/pdf/2607.19547v1"
tags: ["query:long-video"]
score: 10.0
evidence: 组合视觉KV记忆实现长视频时序推理
tldr: 长视频问答需复用视觉KV缓存，但简单拼接各块导致位置冲突，丢失全局时序。ChronoStitch提出训练无关方法：将存储键重基于保留时间、高度、宽度的三轴RoPE，并选择性重计算高偏差后段token以弥补注意力缺口。在Qwen2.5-VL-3B上，事件排序准确率提升，且推理速度较全量重预填充提高3.3倍，验证了高效组合独立视觉记忆的可行性。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.19547v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1082, \"height\": 366, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19547v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1715, \"height\": 368, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19547v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1213, \"height\": 324, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19547v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1406, \"height\": 196, \"label\": \"Table\"}]"
motivation: 独立缓存的视频块KV从位置零开始，直接拼接导致时间相位碰撞，无法回答时序问题。
method: 将存储键重基于全局三轴多模态RoPE坐标系，并选择性重计算高偏差后段视觉token以弥补内容缺口。
result: 在Qwen2.5-VL-3B和TempCompass上，事件排序精度优于基线，且速度比全量重预填充快3.3倍。
conclusion: ChronoStitch为长视频时序推理提供了一种训练无关、高效的视觉KV记忆组合方法。
---

## 摘要
长视频问答要求模型在时间推移中保留视觉证据，而无需反复处理同一视频。一种实用的方法是存储每个视频块对应的视觉-语言模型内部键值（KV）缓存，并在查询时检索这些状态。然而，独立缓存的视频块无法正确组合：每个块都从局部旋转位置零开始预填充，因此简单拼接会冲突时间相位，并移除回答问题所需的全局顺序，例如先发生什么、事件频率或视频中发生了什么变化。本文提出ChronoStitch，一种无需训练的方法，用于组合独立存储的视觉KV记忆。该方法首先将存储的后旋转键重新基于一个全局三轴多模态RoPE坐标系，该坐标系保留了时间、高度和宽度结构。我们展示了一维标量重索引在几何上对于视觉标记是不一致的，因为它将帧内的空间顺序转化为错误的时间位移。然后，我们处理了位置修复留下的残留内容差距：后续块在编码时最初没有关注更早的块。因此，ChronoStitch选择性地重新计算一小部分高偏差的后续块视觉标记，同时允许它们关注组合后的缓存。在Qwen2.5-VL-3B模型和TempCompass的时间分割上，ChronoStitch优于简单的组合和仅位置变体，在提高事件排序准确性的同时，运行速度比完全联合重新预填充快3.3倍。

## Abstract
Long-video question answering requires a model to preserve visual evidence over time without repeatedly reprocessing the same video. A practical approach is to store the vision-language model's internal key-value (KV) cache for each video chunk and retrieve that state at query time. However, independently cached video chunks do not compose correctly: every chunk is prefilled from local rotary position zero, so naive concatenation collides temporal phases and removes the global order required for questions about what happened first, how often events occurred, or what changed across the video. This paper presents ChronoStitch, a training-free method for composing independently stored visual KV memories. The method first re-bases stored post-rotary keys onto a global three-axis multimodal RoPE coordinate system that preserves time, height, and width structure. We show why a one-dimensional scalar re-indexing is geometrically inconsistent for visual tokens because it turns spatial order within a frame into false temporal displacement. We then address the residual content gap left by positional repair: later chunks were originally encoded without attending to earlier chunks. ChronoStitch therefore selectively recomputes a small fraction of high-deviation later-chunk visual tokens while allowing them to attend over the composed cache. On Qwen2.5-VL-3B and the temporal split of TempCompass, ChronoStitch outperforms naive composition and position-only variants, improving event-ordering accuracy while running 3.3x faster than full joint re-prefilling.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
长视频问答系统在面对未知查询时，需要持久地保留有序的视觉证据。直接为每个视频块存储视觉-语言模型的内部 KV 缓存是一种高效替代重复处理的做法，但**独立缓存的块无法直接组合**：
- 每个块都是从局部旋转位置零开始预填充的，简单拼接会导致不同块的**时间相位碰撞**，破坏全局时序。
- 这导致模型无法准确回答“先发生什么”、“事件频率”或“跨视频的变化”等高度依赖时间顺序的问题。
因此，核心问题是：**如何将独立预填充的视觉 KV 缓存以一种保留原有三维时空结构且训练无关的方式组合起来，从而恢复长时域时序推理能力**。

### 2. 论文提出的方法论
ChronoStitch 采用**两阶段、无需训练**的组合修复策略，其关键思想是：先修正位置偏差，再弥补跨块注意力缺失的内容偏差。

- **第一阶段：全局三轴位置重基于（Positional Re‑basing）**
  - 针对多模态 RoPE 中视觉标记具有 `(时间, 高度, 宽度)` 三轴坐标的特点，为每个缓存块分配不重叠的全局坐标区间。
  - 直接对存储的**后旋转键（post‑rotary keys）** 施加 delta 旋转，将它们从局部坐标到全局坐标进行变换，无需恢复原始预旋转键，也无需重新训练。
  - 特别指出：若将二维空间标记展平为一维标量位置，帧内不同图像块的**空间顺序会被错误地编码为时间位移**，导致几何不一致，因此必须保留三轴结构。
- **第二阶段：选择性内容修复（Selective Content Repair）**
  - 位置修正后，后段块在最初编码时**无法关注前段块**，因此产生内容缺口。
  - 采用**基于关键偏差的无需预言机的代理分数**：将缓存组合后进行浅层（一层）重计算，用 `key 的变化量` 衡量每个标记受缺失跨块注意力的影响程度。
  - 选取后段块中偏差最大的前 ρ 比例（实验中使用 ρ=0.35，约占总组合视觉缓存的 25%）的视觉标记，**通过全部 transformer 层重新计算，同时允许它们关注整个组合缓存**，并将其新键值散射回原缓存。
  - 该过程无需重新运行视觉塔，仅对少量标记做重计算，保证了查询时的效率。

### 3. 实验设计
- **基础模型**：`Qwen2.5-VL-3B-Instruct`（36 层，多模态 RoPE 频率分配为 16 时间、24 高度、24 宽度）。
- **数据与基准**：
  - **受控顺序敏感性探针**：将视频块按 A–B 和 B–A 顺序拼接，测试模型能否根据顺序翻转回答。
  - **TempCompass 时间分割**：包含 590 道多选问题，评估“事件排序”和“属性变化”两类时间推理能力。
- **对比方法**（五种策略）：
  1.  完整联合预填充（上限）
  2.  **ChronoStitch**（三轴重基于 + 选择性修复 ρ=0.35）
  3.  仅三轴重基于（无修复）
  4.  一维标量重基于
  5.  简单拼接（无处理）
- **效率实验**：在 12 个视频上测量查询延时。

### 4. 资源与算力
论文明确指出使用 `mlx-community/Qwen2.5-VL-3B-Instruct-4bit` 量化模型，并在 **Apple M 系列硬件**上进行实验。方法本身是完全训练无关的，不涉及任何训练或微调，仅评测推理阶段的延时与精度。文中**未提供 GPU 型号、数量或具体训练时长**，因为整个流程不需要训练算力。

### 5. 实验数量与充分性
- **实验组数**：
  - 层重建保真度诊断（表 1，覆盖层 0 至 35 的余弦相似度与相对 MSE）。
  - 受控顺序探针（不同修复比例 ρ=0.15, 0.35）。
  - TempCompass 基准主实验（表 3，包含总体、事件排序和属性变化）。
  - 查询效率分析（表 4）。
  - 另外包含不同位置策略的消融对比。
- **充分性与客观性**：
  - 实验设计覆盖了诊断（键重建）与实际应用（问答基准），对比公平，消融清晰。
  - 不足之处：仅在一款 3B 小型模型上验证，且 TempCompass 的联合预填充上限仅 63.9%，可观测的改进空间受到压缩；修复比例 ρ 仅基于小型控制实验确定，缺乏跨尺度、跨视频长度的系统调优；效率仅在单一的 Apple M 系列硬件条件下测量，缺乏硬件无关的理论 FLOP 分析。

### 6. 论文的主要结论与发现
- **位置必要但不充分**：三轴重基于在层 0 上可近乎完美地重建联合预填充的键（相对 MSE 达 10⁻⁸ 量级），但仅靠位置对齐几乎无法恢复时序问答能力。一维标量重基于在几何上存在根本性错误，层 0 精度甚至不如简单拼接。
- **内容修复是提升的关键**：在受控顺序探针中，修复 35% 的后段块视觉token 可恢复联合预填充的满分效果；在 TempCompass 上，ChronoStitch 相较于简单拼接总体提升 4.8 个百分点，事件排序提升 7.0 个百分点。
- **效率优势显著**：ChronoStitch 仅需刷新约 1/4 的视觉标记，比完整联合预填充**快 3.3 倍**，同时不需要重新运行视觉塔，实现了高保真记忆组合与低延迟查询的平衡。

### 7. 优点
- **训练无关、即插即用**：无需任何微调或额外训练，直接对已存储的 KV 缓存进行后处理。
- **精确的几何修复**：抓住了多模态 RoPE 的三轴本质，从数学上避免了将空间布局混淆为时间顺序的错误，层 0 重建达到机器精度。
- **代理驱动的高效修复**：利用浅层关键偏差排序，仅重计算受影响最严重的少量标记，在可忽略的精度损失下大幅降低计算开销。
- **两级分离的问题建模**：将组合错误清晰分解为位置错误和内容错误，为后续工作提供了可参照的分析框架。

### 8. 不足与局限
- **模型规模单一**：仅在 3B 参数的量化模型上测试，联合预填充上限较低，结论向更大模型的泛化性尚不明确。
- **修复比例缺乏理论指导**：ρ 值基于小型控制实验经验设定，未论证其在不同块数、不同视频时长下的最优性或自适应策略。
- **效率评估不完整**：仅给出一种硬件上的墙钟时间，未提供 FLOP 分析或不同硬件下的对比，难以准确评估优势的普适性。
- **下游差距未能完全反映位置编码作用**：在 3B 模型上，一维标量重基于与三轴重基于之间的 TempCompass 分数差距不大，暗示几何正确性的优势可能需要在更大或专门设计的模型中才会显著体现。
- **对象仅限视觉缓存**：仅处理视觉前缀的 KV 组合，未讨论文本查询或跨模态交互的缓存修复。

（完）
