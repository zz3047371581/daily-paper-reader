---
title: Efficient Audio-Visual Event Recognition via Knowledge Distillation and Dynamic INT8 Quantization of a Hybrid Cross-Attention Network
title_zh: 基于混合交叉注意力网络的知识蒸馏与动态INT8量化的高效视听事件识别
authors: "Parinaz Binandeh Dehaghani, Danilo Pena, A. Pedro Aguiar"
date: 2026-07-18
pdf: "https://arxiv.org/pdf/2607.16980v1"
tags: ["query:long-video"]
score: 9.0
evidence: 通过高效压缩实现音视频事件检测并便于部署
tldr: "AVER中Transformer架构性能优异，但高开销难部署边缘。本文提出结合模型压缩、知识蒸馏和动态INT8量化的高效框架。该框架以VideoMAE和AST为骨干，通过混合交叉注意力教师模型，缩减学生模型维度、头数和前馈网络，并利用知识蒸馏传递判别知识，最后应用动态INT8量化压缩模型。在AVE数据集上，融合参数减59.06%，精度仅降2.14%，模型大小从10.71MB压至2.04MB。该工作为边缘AI提供精度-效率平衡。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.16980v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1270, \"height\": 847, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16980v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1227, \"height\": 758, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16980v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1225, \"height\": 756, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16980v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1240, \"height\": 1167, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.16980v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1505, \"height\": 765, \"label\": \"Table\"}]"
motivation: 针对视听事件识别模型计算复杂度高、内存占用大，难以部署于边缘设备的问题。
method: 提出结合压缩、蒸馏与动态INT8量化的框架：以VideoMAE、AST和混合交叉注意力为教师，缩减维度、头数的学生模型经蒸馏后量化。
result: "AVE数据集上，融合模块参数减少59.06%，准确率仅降2.14%；量化后模型大小从10.71MB减至2.04MB。"
conclusion: 该框架在精度与计算效率间取得有效平衡，是资源受限边缘AI平台上的有前景部署方案。
---

## 摘要
视听事件识别（AVER）通过基于Transformer的多模态架构已取得显著性能提升。然而，这些模型的高计算复杂度、大内存占用和高推理成本阻碍了其在边缘和资源受限设备上的部署。本文提出了一种针对混合交叉注意力视听事件识别的高效压缩框架，结合了架构模型压缩、知识蒸馏和动态INT8量化。高容量教师模型融合了用于视觉表示学习的VideoMAE、用于音频特征提取的Audio Spectrogram Transformer（AST）以及用于多模态特征融合的混合交叉注意力融合网络。通过减少隐藏特征维度、注意力头数和前馈网络大小，同时保留整体网络架构，构建了一个轻量级学生模型。学生模型使用知识蒸馏进行训练，以有效传递来自教师的判别性知识。最后，应用动态INT8训练后量化进一步减小模型尺寸以实现高效部署。在Audio-Visual Event（AVE）数据集上的实验结果表明，所提出的框架将多模态融合模块中的可训练参数量减少了59.06%，与教师模型相比分类准确率仅下降2.14%。此外，动态INT8量化将模型大小从10.71 MB减少到2.04 MB，同时保持了有竞争力的识别性能。这些结果表明，该框架在识别精度和计算效率之间提供了有效的权衡，使其成为在资源受限的边缘AI平台上部署的有前景的解决方案。

## Abstract
Audio-visual event recognition (AVER) has achieved significant performance improvements through transformer-based multimodal architectures. However, the high computational complexity, large memory footprint, and inference cost of these models hinder their deployment on edge and resource-constrained devices. This paper presents an efficient compression framework for hybrid cross-attention-based audiovisual event recognition by combining architectural model compression, knowledge distillation, and dynamic INT8 quantization. A high-capacity teacher model integrates VideoMAE for visual representation learning, the Audio Spectrogram Transformer (AST) for audio feature extraction, and a hybrid cross-attention fusion network for multimodal feature integration. A lightweight student model is constructed by reducing the hidden feature dimension, the number of attention heads, and the feedforward network size while preserving the overall network architecture. The student model is trained using knowledge distillation to effectively transfer discriminative knowledge from the teacher. Finally, dynamic INT8 post-training quantization is applied to further reduce the model size for efficient deployment. Experimental results on the Audio-Visual Event (AVE) dataset show that the proposed framework reduces the number of trainable parameters in the multimodal fusion module by 59.06%, with only a 2.14% decrease in classification accuracy compared with the teacher model. Furthermore, dynamic INT8 quantization reduces the model size from 10.71 MB to 2.04 MB while maintaining competitive recognition performance. These results demonstrate that the proposed framework provides an effective trade-off between recognition accuracy and computational efficiency, making it a promising solution for deployment on resource-constrained edge AI platforms.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：视听事件识别（AVER）基于Transformer的多模态模型虽能达到高精度，但其计算复杂度高、内存占用大、推理成本高，难以部署在边缘设备、自动驾驶、可穿戴系统等资源受限场景。现有压缩方法多针对通用的视觉或语言模型，未考虑混合交叉注意力融合网络的架构特性，导致多模态交互能力被破坏。
- **整体含义**：提出一种架构感知的压缩框架，专门针对混合交叉注意力视听识别模型，通过减小可训练融合模块的参数量和模型体积，在不显著损失识别精度的前提下，使模型更适用于边缘AI平台。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：保持预训练的VideoMAE和AST骨干不变，仅压缩可训练的多模态融合与分类网络。通过教师-学生框架、知识蒸馏和后训练动态INT8量化实现高效压缩。
- **教师模型架构**：VideoMAE提取视觉特征 → AST提取音频特征 → 可学习的线性投影到公共嵌入空间 → 双向交叉注意力（残差缩放初始化接近零，以稳定早期训练）→ 模态时间注意力聚合 → 分类。
- **学生模型架构压缩**：保留教师模型拓扑，但进行三项结构缩减：
  - 隐藏嵌入维度：512 → 256
  - 多头注意力头数：8 → 4
  - 前馈网络维度：1024 → 512
- **知识蒸馏**：教师参数冻结，学生同时学习真实标签和教师软化的预测分布。损失函数：  
  \(L = (1 - \alpha) L_{CE} + \alpha T^2 L_{KD}\)  
  其中 \(\alpha=0.8\)，\(T=4\)，\(L_{KD}\) 为KL散度。
- **动态INT8量化**：训练后仅对线性层进行动态量化，权重存为INT8，激活值在推理时按分布动态量化，无需重训练，大幅缩减内存占用。

### 3. 实验设计：使用了哪些数据集/场景，它的benchmark是什么，对比了哪些方法
- **数据集**：Audio-Visual Event（AVE）数据集，包含4,143个YouTube视频片段，覆盖28个事件类别（乐器、动物、车辆、人类活动等），每段约10秒，含同步音视频流。
- **划分与预处理**：按官方协议划分训练/验证/测试集；视频被均匀分为10个1秒片段；用冻结的VideoMAE和AST离线提取特征并缓存。
- **对比方法**：教师模型（完整混合交叉注意力）、学生模型（结构压缩+知识蒸馏）、动态INT8量化后的学生模型。论文主要对比三种模型在识别精度、参数量、模型大小、压缩比等方面的表现。

### 4. 资源与算力
- 文中未明确提及所使用的GPU型号、数量或具体训练时长。只说明了使用PyTorch实现，AdamW优化器，最大80个epoch，batch size为4，学习率调度器为ReduceLROnPlateau。因此算力信息在原文中缺失。

### 5. 实验数量与充分性：大概做了多少组实验（如不同数据集、消融实验等），这些实验是否充分、是否客观、公平
- **实验组数**：主要在AVE数据集上比较了三个模型（教师、KD学生、量化学生），提供了训练曲线、归一化混淆矩阵、性能对比表。
- **充分性评估**：
  - 实验展示了参数减少、尺寸缩减、精度下降等核心指标，但**缺少消融实验**（例如单独拆解维度缩减、头数缩减、蒸馏、量化的独立贡献）。
  - 未对比其他压缩方法（如剪枝、纯量化、不同蒸馏策略）或与其他轻量化模型进行横向比较。
  - 仅在单个数据集上验证，未涉及更大规模或更多样化的基准（如AudioSet、Kinetics-Sounds），泛化性证据不足。
  - 总体实验设计客观，但覆盖范围较窄，未充分证明各组件的重要性或框架的广泛适用性。

### 6. 论文的主要结论与发现
- 架构感知压缩学生模型（含知识蒸馏）可将可训练参数量减少59.06%，分类准确率仅下降2.14%。
- 动态INT8量化进一步将模型大小从10.71 MB压至2.04 MB（缩小约5.24倍），精度共损失仅2.99%，推理时虽因动态量化开销略有延迟，但存储需求大幅降低，适合内存受限边缘设备。
- 框架平衡了识别精度与计算效率，证明了结构缩减、知识蒸馏与后训练量化联合应用的有效性。

### 7. 优点：方法或实验设计上有哪些亮点
- **结构保留压缩**：仅压缩融合模块，保持骨干不变，避免破坏预训练表征能力。
- **多技术融合**：将结构压缩、知识蒸馏与动态量化有机结合，而非单一技术堆砌。
- **稳定的交叉注意力初始化**：使用接近零的残差缩放初始化，促进训练稳定，适合学生模型继承。
- **清晰公平的对比**：在相同数据集和设置下对比三个核心变体，指标全面（精度、F1、参数量、模型/检查点大小、压缩比）。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等
- **实验覆盖不充分**：仅在一个中等规模的AVE数据集上评估，未在更大规模或多域数据集上验证。
- **缺少消融和对比实验**：没有单独分析各压缩维度或蒸馏、量化各自的贡献；未与剪枝、量化感知训练等其他压缩方法进行比较。
- **推理延迟问题**：量化后模型在CPU上推理延迟略增，未详细解释或给出优化方案，可能影响实时应用。
- **教师与学生结构固定**：压缩比例基于手工设定的维度缩减，未探索自适应或不同压缩率对精度的影响。
- **骨干特征离线提取**：讨论的压缩仅针对融合部分，整体流程的实际部署仍需考虑离线特征提取的可行性。
- **数据和任务偏差**：AVE数据集事件类别和场景有限，可能无法代表真实复杂边缘环境，泛化性存疑。

（完）
