---
title: Explicit Representation Alignment for Multimodal Sentiment Analysis
title_zh: 面向多模态情感分析的显式表征对齐
authors: "Baode Wang, Ziming Wang, Huacan Wang, Ronghao Chen, Biao Wu"
date: 2026-06-08
pdf: "https://arxiv.org/pdf/2606.09148v1"
tags: ["query:affective-ai"]
score: 9.0
evidence: 利用视觉语言模型对齐文本与图像表征以提升多模态情感分析
tldr: 多模态情感分析常因模态表示不对齐而性能受限，甚至弱于单文本模型。本文提出利用视觉语言模型将图像转化为文本描述，在共享语言空间对齐表示，并结合语义token选择与批次均匀正则化增强鲁棒性。方法在多个基准上取得最优结果，一致超越强基线。研究揭示了表示对齐比融合设计更关键，为可解释的文本中心推理提供了新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.09148v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 776, \"height\": 397, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09148v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1565, \"height\": 646, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09148v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1613, \"height\": 344, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.09148v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 791, \"height\": 573, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.09148v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 794, \"height\": 503, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.09148v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 803, \"height\": 332, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.09148v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 753, \"height\": 296, \"label\": \"Table\"}]"
motivation: 独立预训练模态编码器导致的表示不对齐，是多模态情感模型无法有效发挥多模态优势的核心瓶颈。
method: 利用VLM将视觉内容转为结构化文本，投影到共享语言空间实现显式对齐；并采用语义token选择与批次级均匀正则化混合学习。
result: 在多个多模态情感分析基准上取得最先进性能，一致优于单模态和现有融合基线。
conclusion: 表示对齐比融合复杂性对多模态情感学习更重要，所提策略有效提升鲁棒性与可解释性。
---

## 摘要
多模态情感分析旨在通过联合建模文本和图像等异质模态来理解人类情绪和情感。然而，多模态模型往往未能持续优于强大的纯文本基线，且性能在不同融合策略间差异显著。在本工作中，我们识别出独立预训练模态编码器之间的表征不对齐是多模态学习的关键瓶颈，并通过受控实验证明，融合前的对齐往往比融合复杂度更为重要。为解决这一问题，我们提出了一个统一的多模态情感分析框架，该框架利用视觉-语言模型（VLMs）将视觉内容转化为结构化的文本描述，将异质模态投影到共享的语言空间，从而实现可解释的以文本为中心的推理。为进一步提升鲁棒性，我们引入了一种混合学习策略，将语义token选择与批次级均匀性正则化目标相结合，以促进更分散和稳定的全局特征空间，同时减轻VLM生成描述引入的噪声。在多个多模态情感与情绪基准上的实验表明，我们的方法持续优于强单模态和多模态基线，达到了最先进的性能。我们的分析进一步凸显了表征对齐在多模态情感学习中的关键作用。

## Abstract
Multimodal affective analysis aims to understand human sentiment and emotion by jointly modeling heterogeneous modalities such as text and images. However, multimodal models often fail to consistently outperform strong text-only baselines, with performance varying significantly across fusion strategies. In this work, we identify representation misalignment between independently pretrained modality encoders as a key bottleneck for effective multimodal learning, and show through controlled experiments that alignment prior to fusion is often more important than fusion complexity. To address this issue, we propose a unified multimodal affective analysis framework that leverages vision-language models (VLMs) to convert visual content into structured textual descriptions, projecting heterogeneous modalities into a shared linguistic space and enabling interpretable text-centric reasoning. To further improve robustness, we introduce a hybrid learning strategy that combines semantic token selection with a batch-level uniformity regularization objective, encouraging a more dispersed and stable global feature space while mitigating noise introduced by VLM-generated descriptions. Experiments on multiple multimodal sentiment and emotion benchmarks show that our method consistently outperforms strong unimodal and multimodal baselines, achieving state-of-the-art performance. Our analysis further highlights the critical role of representation alignment in multimodal affective learning.

---

## 论文详细总结（自动生成）

## 论文总结

### 1. 核心问题与整体含义
多模态情感分析试图联合利用文本、图像等异质模态来解析人类情绪，但现有模型常出现一个反常现象：加入视觉信息后的多模态模型并非总能超越**纯文本基线**，性能波动显著且增益微小。  
本文指出，根本瓶颈在于**独立预训练的文本编码器与图像编码器所产生的表征空间存在严重不对齐**（语义几何不兼容、尺度不一致等），导致跨模态交互难以优化，模型实质上退化为文本主导的学习。  
该工作的核心含义是：**融合前的显式表征对齐，往往比设计复杂的融合架构更为关键**。通过对齐表征，可实现更均衡、互补的多模态推理，从而释放多模态信息的真正潜力。

### 2. 方法论
论文提出一个**统一多模态情感分析框架（UMAF）**，包含三个协同组件：

- **VLM 驱动的显式对齐**  
  利用视觉-语言模型（VLM）依据精心设计的提示词（包含 OCR 理解、视觉场景分析、跨模态集成、高层推理四个阶段），将输入图像转换为**结构化的文本描述**（`T_vlm`）。由此，图像和文本被投影到**共享的语言空间**，跨模态交互转化为可解释的“文本-文本”推理。

- **文本中心的统一融合**  
  将原始文本 `T` 与 VLM 生成的视觉代理文本 `T_vlm` 进行 token 级拼接，送入预训练的 RoBERTa 编码，再在其上叠加两层随机初始化的轻量 Transformer（`Transformer_fusion`）来显式建模跨 token 依赖，精细捕捉情感歧义、讽刺等细腻线索。

- **噪声鲁棒的混合学习策略**  
  1. **Top‑K 语义 token 选择**：通过可学习的打分网络 `σ(W_s Z + b_s)` 为融合序列中每个 token 计算重要性得分，仅选取得分最高的 K 个 token 做平均池化（`h_top`），送入分类器并用交叉熵损失（`L_ce`）监督，抑制 VLM 生成中的冗余或噪声 token。  
  2. **批次级均匀性正则化**：对同批次样本的全局表示 `z_i` 施加均匀性损失 `L_uni = (1/B) Σ_i log( Σ_{j≠i} exp( sim(z_i, z_j) / τ ) )`，促使不同样本在表示空间更稀疏、分散，防止特征崩塌并提升稳定性。

最终优化目标为：`L_total = L_ce + λ L_uni`。

### 3. 实验设计
- **数据集**  
  - **MSED**：含 9,190 个图文对，标注了情感极性（3类）、情绪（6类）及欲望（6类），用于多任务情感分析。  
  - **MVSA**：包含 MVSA-Single（4,869 对）和 MVSA-Multiple（16,779 对），用于多模态单/多视图情感分析。

- **对比基线**  
  覆盖多类代表性方法：CoMN、FENet、CM-BERT、MAG、MVAN、ITIN、DMLANet，以及面向 MSED 的 M3GAT、MMTF-DES 等最新模型；消融实验中对比了 BERT+ResNet、RoBERTa+ViT、CLIP 等编码器组合。

- **评价指标**  
  准确率（Accuracy）、精确率（Precision）、召回率（Recall）和 F1 分数。

### 4. 资源与算力
论文**未明确提及**所使用的 GPU 型号、数量、训练时长或具体算力开销。仅说明采用了 RoBERTa-base 作为文本编码器，并在其之上叠加轻量 Transformer 层。作者在局限性中也指出，VLM 生成会引入额外计算成本。

### 5. 实验数量与充分性
实验**较为充分且系统**，包括：  
- **两个主流基准**上的完整性能对比（MSED 情感分析 & 情绪识别，MVSA-Single）。  
- **可控消融实验**：不同编码器组合（文本/图像/多模态）的对比以揭示对齐的重要性；Top‑K 语义选择（K=5,10,20）及去掉噪声鲁棒模块的性能变化。  
- **可视化定性分析**：通过注意力矩阵展示不同对齐策略下的模态偏差与均衡交互模式。  
- 对比方法多样，涵盖早期特征拼接、预训练模型、图网络及最新 VLM 融合方法，比较公平。

### 6. 主要结论与发现
- 所提框架在 MSED 和 MVSA-Single 上**一致取得最优性能**，情感分析 F1 达 89.46%，情绪识别 F1 达 87.09%，MVSA-Single 准确率达 82.3%，均超越强基线。  
- **显式表征对齐是必要前提**：未对齐的模型易出现模态偏差（过度依赖某单一模态），而对齐后模型能均衡关注图文互补线索，提升解释性和预测精度。  
- 语义 token 选择与均匀性正则化共同作用，能有效抑制 VLM 噪声，增强特征空间的判别力和鲁棒性。

### 7. 优点
- **定位关键瓶颈**：明确指出并对齐“表征不对齐”这一常被忽视的问题，提供了受控实验证据。  
- **方法论简洁有效**：将视觉信息转化为文本，天然地消除模态鸿沟，实现可解释的文本中心推理。  
- **鲁棒正则化设计**：Top‑K 选择与批次均匀性正则化的搭配简单而实用，不依赖复杂数据增强或对比对构建。  
- **评估全面**：覆盖情感、情绪等任务，包含定量主表、多样消融和可视化分析，论证链条完整。

### 8. 不足与局限
- **模态覆盖有限**：仅处理文本和图像，未涉及语音、视频等时序模态（论文已明确）。  
- **依赖 VLM 生成质量**：VLM 生成的描述若噪声过多或不完整，会直接影响下游任务；对 VLM 本身的稳健性缺乏深入分析。  
- **泛化性待验证**：仅在 MSED 和 MVSA 两个基准上评估，尚未在更多样化或真实嘈杂场景下证实有效性。  
- **计算开销**：引入 VLM 生成文本描述相比传统直接融合会增加额外计算负担，文中未量化这一代价。

（完）
