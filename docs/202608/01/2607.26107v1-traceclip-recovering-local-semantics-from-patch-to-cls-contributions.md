---
title: "TraceCLIP: Recovering Local Semantics from Patch-to-CLS Contributions"
title_zh: TraceCLIP：从Patch到CLS贡献中恢复局部语义
authors: "Xinran Liu, Shouqian Shi, Yutong Chen, Ge Wang, Xin-Wei Yao, Sheng Zhong"
date: 2026-07-28
pdf: "https://arxiv.org/pdf/2607.26107v1"
tags: ["query:affective-ai"]
score: 9.0
evidence: 从块到CLS贡献中恢复局部语义，实现跨模态表示学习
tldr: CLIP的图像级对比学习侧重于全局对齐，导致局部视觉语言对应缺乏直接约束，制约密集理解任务。TraceCLIP是无训练框架，通过提取CLS注意力输出中的patch特定贡献项恢复局部语义，并转化为语义测地拓扑门以校准末层特征重建。在八个零样本语义分割基准上，平均mIoU超越先前最强训练无关方法1.3至4.5点。该工作揭示了全局表征内部仍可访问空间局部语义，为解读和利用CLIP内在局部信息提供了新视角。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.26107v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 871, \"height\": 710, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.26107v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 816, \"height\": 546, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.26107v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1837, \"height\": 815, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.26107v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 787, \"height\": 731, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26107v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 864, \"height\": 791, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26107v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1282, \"height\": 662, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26107v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 645, \"height\": 570, \"label\": \"Table\"}]"
motivation: CLIP全局对齐缺乏局部区域语义约束，现有训练无关方法未探究CLIP内部何处局部语义最易获取。
method: TraceCLIP分离CLS注意力中的patch贡献项恢复局部语义，并构建语义测地拓扑门校准末层特征重建。
result: 在八个零样本分割基准上，平均mIoU超越最强训练无关方法1.3-4.5点。
conclusion: 表明全局对齐的CLIP内部蕴含空间局部语义，为密集视觉语言理解提供新路径。
---

## 摘要
密集视觉-语言理解，包括目标定位、区域识别和开放词汇语义分割，需要将语言概念与空间锚定的视觉区域相关联。CLIP通过大规模对比预训练学习共享的图像-文本嵌入空间，为这些任务提供了坚实的基础。然而，其图像级目标将文本与CLS衍生的全局表示对齐，使得局部视觉-语言对应关系仅受间接约束。现有方法或引入额外监督、外部模型，或进行任务特定适配，而无需训练的方法主要从现有patch特征中恢复密集响应，却未探究在CLIP中何处局部语义最易获取。我们提出TraceCLIP，一种无需训练的框架，通过分离写入CLS注意力输出的patch特定项，恢复潜在的patch级语义证据。TraceCLIP进一步将贡献衍生的语义响应转化为语义测地拓扑门控，校准用于密集特征重构的最终层patch亲和度。诊断实验表明，这些贡献特征展现出强大的局部语义判别能力和文本条件空间对齐。在八个零样本语义分割基准上，TraceCLIP在两种骨干网络和背景设置下，平均mIoU较先前最强无需训练方法提升了1.3至4.5个百分点，无需额外训练、外部视觉基础模型或区域级监督。更广泛而言，这些发现表明，空间局部化语义可能依然保留在全局对齐表示的内部构造中。

## Abstract
Dense vision-language understanding, including object localization, region recognition, and open-vocabulary semantic segmentation, requires associating language concepts with spatially grounded visual regions. CLIP provides a strong foundation for these tasks by learning a shared image-text embedding space from large-scale contrastive pre-training. However, its image-level objective aligns text with a CLS-derived global representation, leaving local vision-language correspondence only indirectly constrained. Existing methods either introduce additional supervision, external models, or task-specific adaptation, while training-free approaches mainly recover dense responses from existing patch features without examining where local semantics become most accessible within CLIP. We introduce TraceCLIP, a training-free framework that recovers latent patch-level semantic evidence by isolating the patch-specific terms written into the CLS attention output. TraceCLIP further converts contribution-derived semantic responses into a semantic-geodesic topology gate that calibrates final-layer patch affinity for dense feature reconstruction. Diagnostic experiments show that these contribution features exhibit strong local semantic discrimination and text-conditioned spatial alignment. On eight zero-shot semantic segmentation benchmarks, TraceCLIP achieves gains of 1.3 to 4.5 points in average mIoU over the strongest prior training-free methods across both backbones and background settings, without additional training, external vision foundation models, or region-level supervision. More broadly, these findings suggest that spatially localized semantics may remain accessible within the internal construction of globally aligned representations.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

- **核心问题**：CLIP 模型通过图像级对比学习获得了强大的全局图文对齐能力，但缺乏对局部区域（如目标、物体部件）的显式视觉-语言对应监督，导致其在开放词汇密集预测任务（如语义分割、目标定位）上表现受限。现有方法多引入额外训练、监督或模型适配，而未深入探究冻结 CLIP 内部是否已隐含可利用的局部语义信息。
- **整体含义**：本文旨在证明，在冻结的 CLIP 视觉 Transformer 中，从 patch token 到 CLS token 的注意力贡献（patch-to-CLS contributions）是恢复局部语义的最有效切入点，并基于此提出无需训练的密集推断框架 TraceCLIP，为全局对齐模型的内在可解释性与二次利用开辟新路径。

### 2. 论文提出的方法论

- **核心思想**：不依赖额外训练或模型改动，直接从 ViT 的 CLS token 注意力更新中分离出每个图像 patch 的贡献项，将其作为局部视觉-语言特征，并进一步将这些语义响应转化为拓扑门控，校准最终层的 patch 亲和度以重建密集特征。
- **关键技术细节**：
    - **Patch-to-CLS 贡献分解**：对视觉编码器某层的多头自注意力，将 CLS token 对每个 patch \(i\) 在头 \(h\) 的注意力权重 \(A^{l,h}_{0i}\) 与对应值-输出投影矩阵 \(W^{l,h}_{OV}\) 相乘，再经 CLIP 视觉投影 \(W_\text{proj}\) 映射到联合嵌入空间，得到 token-head 贡献 \(C^{l,h}_i\)。汇总所有头得到该层的 patch 贡献特征 \(C^l_i\)。
    - **语义先验构建**：计算贡献特征 \(C\) 与文本嵌入 \(T\) 的点积得到响应矩阵，取最大响应类作为初步语义标签 \(L_i\)，对应的 softmax 概率作为置信度 \(P_i\)。
    - **语义测地拓扑门控**：在四邻接 patch 图上，边界成本 \(w_{ij}=1+\beta\,\mathbf{1}_{L_i\neq L_j} \min(P_i,P_j)\)，计算所有 patch 对之间的最短路径距离 \(D_{ij}\)，再通过高斯核转化为拓扑门控矩阵 \(G_{ij}\)。
    - **亲和度校准与密集重建**：用最终层 query 间的相似度 \(A^h\) 构建 patch 亲和度，以 \(G\) 进行校准得到 \(\tilde{A}^h\)，聚合最终层的 value 特征 \(V^h\)，经输出投影和视觉投影获得密集 patch 特征，与文本嵌入比对后上采样得到分割结果。
- **公式/算法流程**：整体流程为：贡献特征生成 → 初步语义标签与置信度 → 测地距离 → 拓扑门控 → 校准最终层亲和度 → 重建密集特征 → 分类。方法完全冻结两个编码器，无需训练。

### 3. 实验设计

- **数据集与场景**：在8个标准零样本语义分割基准上评估，分为无背景类别（VOC20、Context59、COCO-Stuff、Cityscapes、ADE20K）和有背景类别（VOC21、Context60、COCO-Object）两类设定。
- **Benchmark 与指标**：主要指标为 mIoU，比较对象包括 GroupViT、TCL、CLIP、MaskCLIP、ReCo、SCLIP、ClearCLIP、NACLIP 等无需训练或需要训练的方法。
- **骨干网络**：冻结的 ViT-B/16 和 ViT-L/14。
- **实现细节**：滑动窗口推理（窗口448×448，步长224），logit scale=50，超参数固定为 β=1.0，λ=1.5，σ 按数据集调整，无训练、无测试时优化、无随机采样。

### 4. 资源与算力

- 论文未提及 GPU 型号、数量或训练时长。方法仅涉及冻结模型的推理，无需传统训练，仅需执行多次前向传播和矩阵运算，对算力要求较低但未给出具体消耗数据。

### 5. 实验数量与充分性

- **实验数量**：完成的主要实验包括：
    - 诊断实验：贡献特征在不同层上的区域分类准确率、指向准确率、内外响应差距分析（表1/表2），以及最终块各子模块的语义退化分析。
    - 密集读取分析：直接使用不同层贡献特征进行分割的效果对比（表4a）。
    - 组件消融：对视觉亲和度 \(A^h\)、语义拓扑门 \(G\) 以及校准后 \(\tilde{A}^h\) 的单独与组合效果评估（表4b）。
    - 主结果对比：在8个数据集上与多个无需训练和需训练方法进行系统比较（表3）。
- **充分性与客观性**：实验设计系统，涵盖了诊断性验证、消融、跨骨干和跨数据集比较，对比方法选择公平且覆盖广泛。超参数固定，使用同一套设置完成所有实验，增强了结果的客观性和可复现性。

### 6. 论文的主要结论与发现

- 局部语义证据在 patch-to-CLS 贡献路径中显著强于最终 patch token 状态或 value 特征，且越接近最终层越突出。
- 最终自注意力块主动产生丰富的局部语义，但在与残差 patch 状态混合后变得难以直接获取；MLP 并非主要退化源。
- 贡献特征直接作分割输出可达 25.6 mIoU（ViT-B/16），但仍需空间聚合才能达到最优性能。
- TraceCLIP 通过语义测地门控有效抑制跨语义边界的传播，在8个数据集上均超越现有最强训练无关方法，平均提升 1.3～4.5 点 mIoU，显示全局对齐模型内部蕴含可利用的局部结构化信息。

### 7. 优点

- **无需训练**：完全冻结 CLIP，不引入额外监督或外部模型，部署轻量且易用。
- **新颖的视角**：从注意力贡献路径中恢复局部语义，将解释性工具转化为实际预测机制。
- **框架清晰**：分解、语义先验、几何门控、亲和度校准各模块逻辑明确，超参数固定，易于复现。
- **实验扎实**：涵盖诊断、消融、多骨干、多数据集的系统性验证，结论置信度高。

### 8. 不足与局限

- **计算复杂度**：测地距离计算涉及所有 patch 对的最短路径，尽管网格规模不大，但可能对极高分辨率图像或实时应用带来开销。
- **对新类别及开集场景的泛化性未专门讨论**：贡献特征依赖原文预训练时的类别知识，对长尾类别或开放集性能未做分析。
- **仅限 ViT 架构**：方法基于 transformer 的注意力分解，不能直接迁移到 ConvNet 骨干的 CLIP。
- **无背景类别处理仍有挑战**：虽然在有背景类别上表现好，但背景类别的语义响应机制未被深入探讨，可能依赖阈值等后处理。
- **未探索与训练方法的结合**：可进一步研究将贡献特征用作训练或微调的初始化/正则化信号以提升更强模型。

（完）
