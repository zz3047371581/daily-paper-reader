---
title: "CRAFT: Compression via Recursive Adaptive Fusion of Video Tokens for Vision-Language Models"
title_zh: CRAFT：通过递归自适应融合视频令牌的压缩方法用于视觉语言模型
authors: "Yu Chen, Xiaohong Li, Xiaole Wang, Jianjin Zhang, Jun Sun, Yafeng Deng"
date: 2026-08-03
pdf: "https://arxiv.org/pdf/2608.01644v1"
tags: ["query:long-video"]
score: 9.0
evidence: 递归融合冗余视觉令牌以实现高效的视频视觉语言模型推理
tldr: "视频理解中视觉语言模型需处理海量视觉令牌，导致计算和内存开销剧增，现有压缩方法难以兼顾效率与内容适应。本文提出CRAFT，通过递归自适应融合将令牌选择与可学习融合解耦，利用全局相似性、位置感知加权和通道门控实现查询无关的压缩。在约8倍压缩下，CRAFT保留约97%主干精度，显著提升推理效率，为高冗余视频数据提供高效且保真度的压缩方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.01644v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 845, \"height\": 487, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01644v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1783, \"height\": 788, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01644v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 870, \"height\": 541, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01644v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 874, \"height\": 379, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01644v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 744, \"height\": 824, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.01644v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1823, \"height\": 1170, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.01644v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 878, \"height\": 394, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.01644v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 678, \"height\": 309, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.01644v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 789, \"height\": 843, \"label\": \"Table\"}]"
motivation: 视频令牌冗余度高，现有压缩方法无法在高效压缩的同时保留关键细节与内容适应性。
method: CRAFT递归自适应融合令牌，通过解耦无参数选择与可学习融合，结合位置加权与通道门控，以查询无关方式保持时空坐标。
result: "在多个视频基准上，CRAFT以约8倍压缩率保持97%精度，显著优于先前方法并大幅提升效率。"
conclusion: CRAFT有效平衡压缩比与内容保留，为VLM视频令牌压缩提供新范式，在降低计算成本的同时维持高性能。
---

## 摘要
在视频理解中，视觉语言模型必须处理大量视觉令牌，导致前填充阶段的计算和内存成本急剧上升。这种视觉序列在时空维度上具有高度冗余，但高压缩比往往伴随着关键细节的丢失。现有的令牌压缩方法要么采用启发式、无需训练的压缩方式，内容适应性有限，要么引入额外模块需要进行昂贵的对齐训练，导致效率与适应性之间的权衡仍未解决。为缓解这一局限，我们提出CRAFT：通过递归自适应融合视频令牌的压缩方法。CRAFT通过将无参数令牌选择与可学习令牌融合解耦，递归地合并令牌：全局相似度决定要合并哪些令牌，而位置感知权重模块和内容自适应通道门学习如何融合它们。整个压缩流程与查询无关。由于每个保留的令牌是原始令牌的线性组合，CRAFT保留了其真实的时空坐标，并与预训练语言模型的输入分布保持一致。在多个代表性视频基准上的实验表明，CRAFT持续优于先前最先进的令牌压缩方法。在约8倍压缩下，它保留了骨干网络约97%的平均准确率，并显示出显著的效率提升。

## Abstract
In video understanding, vision-language models (VLMs) must ingest massive numbers of visual tokens, causing the computational and memory cost of the prefill stage to rise sharply. Such visual sequences are highly redundant along the spatio-temporal dimension, yet a high compression ratio is often accompanied by the loss of critical details. Existing token-compression methods either employ heuristic, training-free compression with limited content adaptivity or introduce additional modules that require expensive alignment training, leaving the trade-off between efficiency and adaptivity unresolved. To alleviate this limitation, we propose CRAFT: Compression via Recursive Adaptive Fusion of Video Tokens. CRAFT recursively merges tokens by decoupling parameter-free token selection from learnable token fusion: global similarity determines which tokens to merge, while a position-aware weighting module and a content-adaptive channel-wise gate learn how to fuse them. The whole compression pipeline is query-agnostic. Because every retained token is a linear combination of the original tokens, CRAFT preserves their true spatio-temporal coordinates and stays aligned with the pre-trained language model's input distribution. Experiments on multiple representative video benchmarks show that CRAFT consistently outperforms prior state-of-the-art token-compression methods. At about $8\times$ compression, it retains roughly $97\%$ of the backbone's average accuracy and shows significant efficiency improvement.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义
- **研究背景**：视频理解的视觉语言模型（VLMs）需处理大量视觉令牌（约10^4量级），在前填充阶段自注意力计算复杂度呈二次增长，解码阶段KV缓存内存占用线性累积，成为效率瓶颈。视觉序列在时空上高度冗余，但高压缩比常导致关键细节丢失。
- **现有方法不足**：
  - **训练无关启发式方法**：如硬丢弃或等权平均，无训练成本，但合并权重固定，缺乏内容适应性，在复杂动态特征下易损害理解能力。
  - **可学习压缩方法**：引入参数化模块重新编码视觉输入，拟合能力强，但需昂贵对齐训练，重构表示偏离预训练分布，且部分方法依赖查询条件，无法复用KV缓存。
- **核心问题**：如何在保持低训练开销和通用可复用性的同时，赋予压缩操作内容适应性，平衡效率与保真度。

## 2. 方法论
- **核心思想**：CRAFT采用“先选择再融合”的递归迭代机制，解耦令牌压缩为两部分：
  - **选择哪些令牌合并**：训练无关的全局相似度动态决定（参数无关、查询无关）。
  - **如何合并**：端到端可学习的门控合并机制，包括全局位置感知权重和逐通道局部门控。
- **关键技术细节**：
  - **训练无关全局选择**：利用随机投影（Johnson-Lindenstrauss性质）在低维空间（128维）近似余弦相似度，避免高维计算。每轮进行配对、预算截断、方向确定和链式预防，保留ℓ₂范数更大的令牌作为代表，确保空间坐标保留。
  - **可学习门控融合**：对于代表令牌\(x_t\)及其源令牌集合\(\{x_i\}\)，融合方式为：\(x_t^{new} = x_t - \sum_i w_i G_i \odot (x_t - x_i)\)。
    - **全局位置感知权重\(w_i\)**：通过低秩投影和3D旋转位置编码（M-RoPE）建模时空关系，结合选择阶段的相似度，经softmax归一化得到自适应权重。
    - **局部内容条件门控\(G_i \in (0,1)^D\)**：基于代表和源令牌拼接，经adaLN（以合并轮次调制）和Sigmoid输出通道级门控，控制每个通道的贡献。初始化为0.5，采用零初始化的adaLN-Zero确保稳定训练。
  - **算法流程**：递归进行选择与融合，直到达到相似度阈值或固定压缩比，支持自适应阈值和固定比例两种模式。整个过程查询无关，一次压缩可复用于多轮对话。
- **公式示例**（文字描述）：
  - 相似度计算：\(E_{sim} = \text{norm}(X W_{rand})\)，\(s_{ij} \approx \cos(x_i, x_j)\)。
  - 权重计算：\(w_i = \text{softmax}((\hat{Q}_i \hat{K}_t^\top + s_{ti}) / \tau)\)。
  - 门控生成：\(G_i = \sigma(W_{up} \tilde{h}_i)\)，其中\(\tilde{h}_i\)由adaLN调制。

## 3. 实验设计
- **数据集/场景**：在6个视频理解基准上评估：Video-MME、LongVideoBench、MVBench、EgoSchema、MLVU、TempCompass，覆盖短片段到长视频时间推理。
- **骨干模型**：主要使用Qwen3.5-4B，并迁移至Qwen2.5-VL-7B和LLaVA-OV-7B以展示跨架构通用性。
- **对比方法**：
  - 训练无关方法：DivPrune、VidCom^2、LLaVA-Scissor、VisionZip、FastVID。
  - 可学习方法：TimeChat-Online、VisionSelector（在Qwen2.5-VL-7B上对比）。
- **评估指标**：不同压缩比下的Top-1准确率、准确率保留率、实际压缩比、前填充延迟、KV缓存大小、FLOPs减少。

## 4. 资源与算力
- **训练硬件**：单节点8块NVIDIA A800 (80GB) GPU。
- **训练配置**：两阶段课程（字幕预训练+问答微调），仅优化约5M参数的合并模块，冻结视觉编码器、投影器和LLM。使用AdamW，余弦学习率衰减，warmup比例0.02，每设备批量1，梯度累积4（全局批量32），混合精度bf16，DeepSpeed ZeRO-2，梯度检查点。第一阶段学习率4e-3，第二阶段6e-6。训练1个epoch。文中未明确总训练时长。
- **其他**：在训练中随机采样压缩设置（相似度阈值或固定比例），使单检查点泛化到多种预算。

## 5. 实验数量与充分性
- **主要实验**：在6个基准上的压缩性能对比（约8×），包含与多种训练无关方法的头对头比较，以及跨3种骨干网络的迁移实验。
- **效率分析**：前填充延迟、KV缓存、FLOPs减少的定量比较。
- **消融实验**：通过渐进式消融验证权重规则（均值、目标修剪、门控）和两阶段训练的作用。
- **模块解耦分析**：评估选择脚手架与可学习合并各自贡献，包括将合并模块嫁接至不同脚手架（如Scissor-Merge）。
- **压缩比扫描**：从2×到32×的准确率退化曲线。
- **结论**：实验设计充分，覆盖多种压缩设置、骨干结构、剥离对比，结果公平且消融细致，能支持方法有效性论断。

## 6. 主要结论与发现
- CRAFT在约8倍压缩下，保留骨干网络平均准确率的96.8%（65.3 vs 67.4），在所有训练无关基线中表现最优，且在某些基准上甚至超过未压缩模型。
- 随着压缩比增大，CRAFT准确率下降较慢，与训练无关方法差距扩大。
- 方法可跨骨干迁移，无需架构特定调优，显示通用性。
- 效率上，CRAFT实现最低前填充延迟、最小KV缓存和最大FLOPs缩减。
- 消融表明可学习门控和位置感知权重对恢复被丢弃信息至关重要。

## 7. 优点
- **创新性**：首次在通用视觉令牌压缩中引入端到端可迭代的“选择并融合”范式，解耦无参选择与可学习融合。
- **高效率与低开销**：仅新增5M参数，无需重新对齐；相似度计算在低维随机投影空间进行，开销低。
- **查询无关且分布保持**：压缩令牌保留原始时空坐标，为原始令牌线性组合，避免分布偏移，且可复用缓存。
- **内容自适应性强**：全局权重和通道门控提供双重调节，优于固定融合规则。
- **实验扎实**：多基准、多骨干、多角度的对比与消融，且训练数据与评测集无重叠（视频级去重）。

## 8. 不足与局限
- **实验覆盖**：仅在3个中小规模VLM骨干上验证，未在更大规模模型（如70B+）或纯视频架构上测试。
- **训练数据依赖**：虽然采用公开数据集并去重，但未给出更详细的数据分布统计可能影响可复现性。
- **压缩比与任务适应性**：对时序精度要求极高的任务（如精确时间定位）效果未评估，高压缩比下可能丢失细粒度时间信息。
- **方法复杂度**：两阶段训练和随机采样预算策略增加训练工程复杂度，训练时间未明确报告。
- **社区偏差风险**：所有基准为英文或多模态问答，可能在其他语言或开放场景下泛化性未知。

（完）
