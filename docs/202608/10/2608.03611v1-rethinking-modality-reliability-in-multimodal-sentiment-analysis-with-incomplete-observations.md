---
title: Rethinking Modality Reliability in Multimodal Sentiment Analysis with Incomplete Observations
title_zh: 重新思考不完整观测下多模态情感分析中的模态可靠性
authors: "Chunlei Meng, Jacqueline J. Pang, Pengbin Feng, Zhenyu Yu, Chun Ouyang, Zhongxue Gan"
date: 2026-08-04
pdf: "https://arxiv.org/pdf/2608.03611v1"
tags: ["query:affective-ai"]
score: 9.0
evidence: 不完整观测下的多模态情感分析，显式建模模态可靠性。
tldr: 不完整多模态情感分析中，模态可靠性往往被隐式处理，引发可靠性不匹配和传播偏差。本文提出MRCF框架，显式建模样本级可靠性：可靠性感知分支从模态内质量和跨模态一致性估计可靠性；可靠性引导交互分支据此调制信息流；可靠性校准融合模块整合两者进行预测。在CMU-MOSI、CMU-MOSEI和CH-SIMS上取得领先性能，证实显式建模可有效缓解上述问题。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.03611v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 877, \"height\": 263, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.03611v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1651, \"height\": 499, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.03611v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 877, \"height\": 275, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.03611v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 757, \"height\": 470, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.03611v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 857, \"height\": 355, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.03611v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1722, \"height\": 564, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.03611v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 865, \"height\": 388, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.03611v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 832, \"height\": 1038, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.03611v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 831, \"height\": 818, \"label\": \"Table\"}]"
motivation: 现有方法隐式处理模态可靠性，导致可靠性不匹配和传播偏差，影响不完整多模态情感分析性能。
method: 提出MRCF框架，包含可靠性感知分支（从模态内质量和跨模态一致性估计可靠性）、可靠性引导交互分支（调制跨模态信息）和可靠性校准融合模块。
result: 在CMU-MOSI、CMU-MOSEI和CH-SIMS数据集上，不完整观测协议下取得先进性能。
conclusion: 显式建模模态可靠性有助于缓解可靠性不匹配和传播偏差，提升不完整多模态情感分析的鲁棒性和准确性。
---

## 摘要
多模态情感分析（MSA）整合文本、音频和视觉来推断人类情感，但现实世界的多模态观测往往不完整。现有针对不完整观测MSA的方法主要遵循两种范式。基于重构的方法从观测到的模态中恢复缺失信息，而联合表示方法则直接从不完整输入中学习。尽管这些方法有效，但它们通常只在表示学习或融合设计中隐式地处理模态可靠性，而非显式建模。我们认为模态可靠性是不完整观测设置中的核心变量。未能显式建模会导致两个相关问题。第一个是可靠性不匹配，即各模态保留的情感证据因样本和缺失率而异。第二个是可靠性传播偏差，即来自退化模态的信息可能对跨模态交互和预测性能产生不利影响。为解决这些问题，我们提出MRCF，一个面向不完整观测MSA的模态可靠性校准框架。MRCF包含一个可靠性感知分支，从模态内质量线索和跨模态语义一致性估计样本特定的模态可靠性；一个可靠性引导的交互分支，使用估计分数调节跨模态信息流；以及一个可靠性校准的融合模块，整合可靠性和语义线索进行最终预测。在CMU-MOSI、CMU-MOSEI和CH-SIMS上的实验表明，MRCF在标准的不完整观测协议下取得了强劲的性能。进一步分析证明，显式的可靠性建模有助于在交互和融合过程中缓解可靠性不匹配和可靠性传播偏差。

## Abstract
Multimodal Sentiment Analysis (MSA) integrates text, audio, and vision to infer human affect, yet real-world multimodal observations are often incomplete. Existing methods for incomplete-observation MSA mainly follow two paradigms. Reconstruction-based methods recover missing information from observed modalities, while joint-representation methods learn directly from incomplete inputs. Although effective, these methods usually treat modality reliability only implicitly within representation learning or fusion design rather than modeling it explicitly. We argue that modality reliability is a central variable in incomplete-observation settings. Failure to model it explicitly gives rise to two related issues. The first is reliability mismatch, in which the affective evidence retained by each modality varies across samples and missing rates. The second is reliability propagation bias, in which messages from degraded modalities may adversely affect cross-modal interaction and predictive performance. To address these issues, we propose MRCF, a Modality Reliability-Calibrated Framework for MSA with incomplete observations. MRCF contains a Reliability-Aware Branch that estimates sample-specific modality reliability from intramodal quality cues and cross-modal semantic consistency, a Reliability-Guided Interaction Branch that uses the estimated scores to modulate cross-modal information flow, and a Reliability-Calibrated Fusion Module that integrates reliability and semantic cues for final prediction. Experiments on CMU-MOSI, CMU-MOSEI, and CH-SIMS show that MRCF achieves strong performance under standard incomplete-observation protocols. Further analyses provide evidence that explicit reliability modeling helps mitigate reliability mismatch and reliability propagation bias during interaction and fusion.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **研究背景**：多模态情感分析（MSA）依赖文本、音频和视觉信息的互补性来推断情感。但现实中传感器故障、遮挡、隐私限制等因素常导致输入模态不完整（观测缺失）。
- **核心问题**：现有针对不完整观测的方法（如重构缺失信息或学习联合表示）大多**隐式地处理模态可靠性**，即它们没有将“当前各模态提供的信息有多可信”作为一个显式的、样本级变量来建模。
- **关键矛盾与含义**：作者指出，隐式处理引发了两个系统性偏差：
    - **可靠性不匹配**：不同模态随缺失率增加，其保留的情感证据衰减速度不同，导致“最可靠模态”动态变化。
    - **可靠性传播偏差**：低可靠性（退化模态）的信息进入跨模态交互后，其噪声会污染整体联合表示，甚至产生负迁移。
- **整体含义**：本文主张将**模态可靠性**提升为不完整MSA中的**第一性变量**，通过显式估计并利用它来指导交互与融合，从根本上缓解上述两个问题。

### 2. 论文提出的方法论
- **总体框架（MRCF）**：提出了一种模态可靠性校准框架，由三个紧密协作的模块构成：可靠性感知分支、可靠性引导的交互分支和可靠性校准的融合模块。
- **可靠性感知分支（RAB）**：
    - **目的**：估算当前样本下各模态的可靠性得分 \(\rho\)（0到1之间）。
    - **关键线索**：
        - **模态内质量线索**：将该模态的池化表征与观测比例（未被mask的比例）拼接后映射得到。
        - **跨模态语义一致性线索**：计算该模态表征与其他模态表征的余弦相似度的均值。
    - **可靠性估计**：将以上两种线索通过非线性映射和sigmoid函数融合，得到最终得分。
    - **训练监督**：利用训练时可用的完整观测数据构建**代理可靠性目标**。该目标定义为不完整单模态预测与完整观测融合预测之间的一致性（指数化负绝对差）。使用平滑 L1 损失和排序损失（使高可靠性模态得分高于低可靠性模态）进行学习。
- **可靠性引导的交互分支（RGIB）**：
    - **目的**：在跨模态交互过程中，根据源模态的可靠性得分调节信息流，抑制低可靠性模态的噪声传播。
    - **机制**：
        - 计算源模态 \(n\) 到目标模态 \(m\) 的常规交叉注意力消息。
        - **源端路由加权**：根据源模态 \(n\) 的可靠性计算路由权重 \(\alpha_{m\leftarrow n}\)（可靠性越高权重越大）。
        - **目标端更新控制**：将目标模态 \(m\) 的更新幅度与其可靠性挂钩。若目标模态本身可靠性高，则更新幅度小（保护高质量信息），反之则允许更多更新，并优先吸收更高可靠性源模态的信息。
    - **正则化**：使用可靠性加权的消息能量正则项，显式惩罚来自低可靠性源模态的消息强度。
- **可靠性校准的融合模块（RCFM）**：
    - **目的**：在最终决策时，同时考虑模态的语义表征和已估计的可靠性，并利用完整观测表征作为训练校准锚点。
    - **机制**：
        - 融合权重通过拼接语义表征和可靠性得分共同计算。
        - **训练校准**：将不完整观测的融合表征向完整观测的融合表征（使用停止梯度）对齐，使用均方误差损失，旨在保留有价值互补信息并防止过度偏离。
- **训练目标**：总损失 = 主任务回归损失 + 可靠性感知分支损失 + 传播正则损失 + 融合校准损失。推理时仅使用不完整观测，无需完整观测分支。

### 3. 实验设计
- **数据集与场景**：使用了三个广泛使用的多模态情感分析基准数据集：**CMU-MOSI**、**CMU-MOSEI** 和 **CH-SIMS**。
- **缺失协议**：
    - **模态内随机缺失（IMM）**：随机擦除时序模态中不同比例 \(r \in \{0.0, 0.1, ..., 0.9\}\) 的时间步，模拟细粒度信息部分缺失。
    - **固定模态缺失（FMM）**：整个模态在推理时完全不可用（如只有视觉+音频），模拟粗粒度的模态级缺失。
- **对比的基线方法**：对比了广泛的SOTA方法，包括：
    - 通用MSA方法：MISA, S-MM, MMIM, CENet, ALMT。
    - 不完整观测专用方法：TFR-Net, LNLN, P-RMF, DAR, MIG-HCL, CoKD, ROSA 等。
    - 缺失鲁棒性相关方法：NIAT, GCNet, IMDer, EMT, GMD 等。

### 4. 资源与算力
- **硬件**：所有实验在 **单张 NVIDIA A100 GPU** 上运行。
- **训练配置**：使用 PyTorch 框架，优化器为 Adam，学习率 \(10^{-4}\)，批大小（batch size）为 32，总共训练 **100 个 epoch**。论文未提及具体的总训练时长（小时/分钟）。
- **特征提取**：文本使用 BERT 作为骨干网络，音频和视觉特征遵循已有工作标准流程提取。

### 5. 实验数量与充分性
实验设计**相当充分且系统**，主要体现在：
- **多协议覆盖**：在两个核心缺失协议（IMM 和 FMM）下均进行了评估，并与大量基线（>10种）比较。
- **多数据集验证**：在三个主流英文和中文数据集上验证了方法的泛化性。
- **全面的消融研究**：详细拆解了核心组件（移除RAB、RGIB、RCFM及其组合）、可靠性估计策略（移除模态内质量、跨模态一致性、排序损失）、交互/融合变体（统一交互、延迟加可靠性、加性/拼接融合）以及各训练目标（移除 \(L_{rel}\)、\(L_{prop}\)、\(L_{cal}\)）的贡献。
- **深入的进一步分析**：
    - 与预测一致性诊断评分的秩相关性分析。
    - 交互行为可视化（高低可靠性间消息流动比率）。
    - 多次种子下的统计稳定性（箱线图）分析。
- **公平性**：遵循了已有工作的标准协议（如对缺失率和分区的结果取平均），确保了比较的客观性。

### 6. 论文的主要结论与发现
- **显式建模可靠性至关重要**：将模态可靠性作为一个独立且可学习的样本级变量，能有效解决不完整观测下的“可靠性不匹配”和“可靠性传播偏差”。
- **MRCF框架有效性**：所提出的框架在多个数据集、多种缺失模式下均取得了最优或最具竞争力的结果，证明了其鲁棒性。
- **机制验证**：
    - 估计的可靠性得分与基于预测一致性的代理指标正相关。
    - 可靠性引导的交互确实使信息流动偏向高可靠性模态，且这种偏向随缺失加剧而增强。
    - 融合校准和传播正则化等各组件及训练目标均对性能有正面贡献。
    - 模型在多次实验中表现出更稳定的预测性能分布。

### 7. 优点
- **问题定义新颖且深刻**：明确指出了现有方法“隐式处理可靠性”的不足，并系统性地定义了两个相关偏差（不匹配与传播偏差），为领域提供了新的研究视角。
- **方法论设计精巧**：框架的三大模块（感知、引导、校准）逻辑递进，将“估计可靠性”与“利用可靠性”紧密耦合，实现了从信号质量评估到决策优化的闭环。
- **训练策略巧妙**：利用训练时极易获得的完整观测数据构建**代理可靠性目标**作为一种“弱监督”，无需昂贵的人工标注，方法简洁有效且易推广。
- **实验说服力强**：实验设计环环相扣，不仅展示了最终性能的优越，还通过丰富的消融、可视化和统计分析揭示了方法的内在机理，多重验证了其动机和假设。

### 8. 不足与局限
- **代理目标的有效边界**：可靠性代理目标依赖于单模态预测与完整观测预测的一致性。当完整观测自身的融合预测存在偏差，或所有单模态预测均严重偏离时，该代理目标的指导意义可能下降，这一定性未在文中深入讨论。
- **对缺失模式的泛化性**：实验仅考察了随机时序缺失和完整模态缺失两种极端模式，对于现实世界中更复杂的缺失模式（如连续片段缺失、非随机缺失）的表现尚不明确。
- **训练数据要求**：方法在训练时依赖于**成对的不完整-完整观测样本**（同一时刻的完整模态和缺失模态版本）。在某些真实场景下，同时采集完整和不完整数据的成本可能很高。
- **计算开销分析缺失**：论文未将所提方法的参数量、浮点运算次数或推理延迟与基线模型进行对比，显式估计和引导机制可能引入额外开销，其实用性评估略显不足。
- **统计显著性检验**：尽管展示了均值和稳定性，但并未报告与最优基线对比的显著性检验结果（如p值），稍欠严谨。

（完）
