---
title: "MAF: Multimodal Adaptive Few-shot Prompting for Sentiment Analysis with MLLMs"
title_zh: MAF：面向MLLMs情感分析的多模态自适应少样本提示
authors: Hangling Xie
date: 2026-06-14
pdf: "https://arxiv.org/pdf/2606.15694v1"
tags: ["query:affective-ai"]
score: 10.0
evidence: 提出面向多模态情感分析的自适应少样本提示，动态利用多模态大语言模型及面部、场景、文本线索
tldr: 针对多模态大语言模型在情感分析中提示敏感且静态提示不足的问题，提出MAF自适应少样本提示框架。MAF构建多模态示例检索模块，联合编码面部表情、场景、文本语义，通过唇动幅度检测准确识别说话人，训练轻量网络实时生成融合权重以选取Top-K信息丰富示例，并运用多数投票增强预测稳定性。在公开基准上，MAF显著且一致地超越相应骨干模型，与强基线相比具备竞争力，为MLLM情感分析提供了高效自适应的提示新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.15694v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1266, \"height\": 343, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15694v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1272, \"height\": 485, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15694v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1271, \"height\": 843, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15694v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1142, \"height\": 610, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.15694v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1273, \"height\": 196, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15694v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1270, \"height\": 987, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15694v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1264, \"height\": 305, \"label\": \"Table\"}]"
motivation: MLLMs在情感分析中性能极度敏感于提示设计，静态提示无法捕捉不同输入的多模态细微差异。
method: 提出MAF，动态检索多模态示例，运用唇动检测识别说话人，自适应融合权重选取Top-K提示，并多数投票。
result: 在多个基准上，MAF超越骨干变体，取得一致显著提升，且与强多模态情感分析基线竞争。
conclusion: MAF通过自适应少样本提示有效克服了MLLM提示敏感问题，为多模态情感分析提供了灵活且性能优越的解决方案。
---

## 摘要
多模态大语言模型（MLLMs）在理解复杂多模态内容方面展现出显著能力。然而，它们在情感分析中的性能对提示设计高度敏感，静态、统一应用的提示本质上无法捕捉不同输入之间变化的细微多模态线索。为解决这一局限，我们提出多模态自适应少样本提示（MAF）框架，动态检索并整合与查询相关的示例，以上下文敏感的方式激发MLLMs的情感推理能力。MAF构建了一个演示检索模块，全面编码面部表情、场景上下文和文本语义，并引入唇动幅度检测机制，用于在多人场景中准确识别说话者。与传统固定权重融合不同，我们训练一个轻量级系数生成网络，实时输出查询条件融合权重，从而对多模态相似度得分进行加权聚合，以检索前K个最具信息量的演示。通过MLLM生成的多个候选输出进行多数投票，进一步增强了预测稳定性。在公开基准数据集上的大量实验表明，与相应的骨干变体相比，MAF实现了大幅且一致的性能提升，并与强多模态情感分析基线保持竞争力。

## Abstract
Multimodal large language models (MLLMs) have demonstrated remarkable capabilities in understanding complex multimodal content. However, their performance in sentiment analysis exhibits acute sensitivity to prompt design, rendering static, uniformly applied prompts inherently suboptimal for capturing the nuanced multimodal cues that vary across inputs. To address this limitation, we propose a Multimodal Adaptive Few-Shot Prompting (MAF) framework, which dynamically retrieves and integrates query-relevant demonstrations to elicit the sentiment reasoning capabilities of MLLMs in a context-sensitive manner. MAF constructs a demonstration retrieval module that holistically encodes facial expressions, scene context, and textual semantics, with a lip movement amplitude detection mechanism introduced for accurate speaker identification in multi-person scenarios. Departing from conventional fixed-weight fusion, a lightweight coefficient generation network is trained to output query-conditioned fusion weights in real time, enabling weighted aggregation of multimodal similarity scores to retrieve the top-K most informative demonstrations. Prediction stability is further enhanced through majority voting over multiple candidate outputs generated by the MLLM. Extensive experiments on public benchmark datasets demonstrate that MAF achieves substantial and consistent performance improvements over the corresponding backbone variants and remains competitive with strong multimodal sentiment-analysis baselines.

---

## 论文详细总结（自动生成）

好的，作为资深学术论文分析助手，我将对给定的论文进行结构化深入总结。

### 1. 论文的核心问题与整体含义

*   **核心动机与背景**：多模态大语言模型（MLLMs）在情感分析中展现出巨大潜力，但其性能对**提示（Prompt）的设计极为敏感**。传统的静态、一刀切的提示方法难以捕捉不同输入样本中表情、场景、文本等动态变化的细微多模态线索，导致预测结果不佳甚至不稳定。
*   **研究的根本问题**：如何设计一种动态的、上下文敏感的方法，为每个特定的多模态输入（查询）生成自适应的提示，从而可靠地激发MLLMs的情感推理能力。
*   **整体含义**：该论文提出了一种从“静态提示”转向“自适应提示”的范式。其核心思想不是微调庞大的MLLM本身，而是通过在输入端动态检索和融合最相关的少样本示例（Few-shot examples），以外挂的方式引导模型进行更精准的推理。

### 2. 论文提出的方法论：MAF框架

MAF框架不更新MLLM参数，而是通过检索增强生成（RAG）与轻量级可训练模块的结合，实现动态示例检索与提示构建。其核心包含三个模块：

*   **混合演示检索模块**
    *   **核心思想**：全面编码并检索与查询最相关的多模态演示示例。
    *   **关键技术细节**：
        *   **多特征提取**：从视频帧和文本中分别提取三类特征：**面部特征**（使用OpenFace）、**场景特征**（使用PlaceCNN）和**文本特征**（使用LLM文本编码器）。视频级特征通过时序平均池化获得。
        *   **说话人识别**：针对多人场景，提出一种创新性的基于**唇动幅度检测**的机制，通过计算上下唇关键点欧氏距离变化来定位活跃说话者。
        *   **相似度计算**：分别计算查询与候选示例在三类特征上的内积相似度。
    *   **算法流程**：最终检索得分是三种相似度的加权和：`srank = α1*面部相似度 + α2*场景相似度 + α3*文本相似度`，并按此得分排序选出Top-K个最相关的演示。

*   **自适应系数生成模块**
    *   **核心思想**：打破传统固定权重融合的局限，为每个查询动态生成最优的融合系数（α1, α2, α3）。
    *   **关键技术细节**：
        *   **模型架构**：一个轻量级的**两层MLP**网络。输入是拼接后的查询多特征向量（4096维），输出是每个预定义权重组合的得分。
        *   **权重离散化**：将连续权重空间以0.1为步长离散化为66组（满足α1+α2+α3=1）三元组。
        *   **训练目标**：这是一个**多标签分类问题**。训练目标是二元交叉熵损失，其标签为“使用某组权重组合能否让最终预测正确”。推理时直接选取得分最高的权重组合。

*   **多数投票模块**
    *   **核心思想**：稳定MLLM概率性解码带来的预测波动。
    *   **算法流程**：对于同一个查询，让MLLM执行M次采样，生成M个候选情感标签，然后通过投票选出出现次数最多的标签作为最终结果。

### 3. 实验设计

*   **数据集**：实验覆盖了三个具有代表性且互补的公开基准数据集：
    *   **CMU-MOSEI**：英文单人独白，情感强度回归（[-3, +3]）。
    *   **CH-SIMS v2.0**：中文多样场景，情感强度回归（[-1, +1]）。
    *   **MELD**：英文多人对话，七分类情感识别。
*   **基准（Benchmark）**：对比了大量的SOTA模型，涵盖了早期融合、后期融合、基于Transformer、基于图网络以及同样使用LLM/MLLM的方法。例如：TFN、MulT、MAG-BERT、MSE-Adapter、ATGFB等。
*   **评估指标**：依据数据集特性采用不同指标，包括七分类准确率(Acc-7)、二分类准确率(Acc-2)、F1值、平均绝对误差(MAE)、相关系数(Corr)、加权F1(WF1)等。
*   **对比方法**：核心对比是在同一MLLM骨干（Qwen-1.8B, LLaMA2-7B, ChatGLM3-6B）下，将MAF与现有的MLLM情感分析方案（MSE-Adapter, ATGFB）进行对比，以验证其作为通用提示框架的有效性。

### 4. 资源与算力

*   **硬件**：文中明确指出，所有实验均在 **NVIDIA GeForce RTX 4090** 单卡上运行。
*   **训练细节**：需要训练的仅是为系数生成模块设计的轻量级MLP网络。文中未提及整体的端到端训练或MLLM的参数微调，因此算力消耗主要源于MLLM的推理过程。MLP的训练优化器为Adam，初始学习率`10^-3`，dropout率为`0.2`。
*   **推理优化**：使用FAISS向量数据库加速演示检索。对于多数投票模块，采用了并行API调用，使得生成多个候选结果的延迟接近于单次生成。

### 5. 实验数量与充分性

*   **实验量**：实验比较充分。
    *   **主实验**：在3个数据集、3个不同LLM骨架上的性能全面对比（Table 2），共涉及超过15种基线方法。
    *   **消融实验**：系统地分析了框架的四大核心组件（检索增强生成、自适应检索、自适应权重、多数投票）的贡献，结果清晰（Table 3）。
    *   **超参数分析**：对关键超参数K（检索示例数）和M（投票候选数）进行了敏感性分析，并给出了明确的取值建议（Figure 4）。
*   **客观性与公平性**：实验设计较为客观公平。核心对比均在相同MLLM骨干上进行，有效隔离了模型自身能力的影响，突出了MAF框架本身的增益。使用了固定随机种子确保可复现性。

### 6. 论文的主要结论与发现

*   MAF作为一种通用的提示框架，能显著且一致地提升不同MLLM（Qwen, LLaMA2, ChatGLM3）在多模态情感分析任务上的表现。
*   演示的**质量与相关性远比简单引入示例重要**。自适应检索和自适应加权能够有效挑选出对当前查询最有信息量的演示，错误的演示会误导模型。
*   框架内部的三个组件（高质量检索、动态权重、投票集成）是**互补增益**的，每个组件都对最终性能有正面贡献。
*   通过多数投票机制，可以有效克服MLLM概率性输出不稳定的问题，提高预测的鲁棒性。

### 7. 优点

*   **范式创新**：将问题从“设计更好的静态提示”升级为“设计动态生成提示的机制”，为MLLM的应用提供了新思路。
*   **轻量高效**：不微调庞大的MLLM，仅训练一个微型网络（MLP），极大降低了计算和部署成本。
*   **方法设计精巧**：
    *   **唇动检测**机制简单而有效，解决了多人视频中的说话人定位难题。
    *   **系数生成器**将连续权重优化问题巧妙地转化为多标签分类问题，训练方案务实。
*   **实验扎实可靠**：覆盖了多种语言、任务类型和模型骨架，消融和参数分析完整，结论具有说服力。

### 8. 不足与局限

*   **模态覆盖不全**：论文主要关注视觉（面部、场景）和文本，尽管提到了“听觉”是情感的关键组成部分，但本框架中并未包含声学特征，这可能限制了其在语音情感丰富场景下的能力上限。
*   **检索效率与扩展性**：虽然利用了FAISS，但随着演示库规模的急剧扩大，多模态特征混合检索的实时性是一个潜在挑战。论文实验仅随机选取了1000个演示构建库。
*   **特征提取依赖**：面部和场景特征的提取分别依赖于OpenFace和PlaceCNN等第三方模型，这些模型的性能上限和领域适应性可能会成为MAF整体性能的瓶颈。
*   **权重离散化**：将权重空间离散为66个组合是一种简化，可能存在更优的连续权重未在搜索空间内，导致性能未达到理论最优。
*   **投票随机平局处理**：对票数持平的情况进行随机选择，这在一个高可靠性要求的系统中可能引入不必要的不确定性。

（完）
