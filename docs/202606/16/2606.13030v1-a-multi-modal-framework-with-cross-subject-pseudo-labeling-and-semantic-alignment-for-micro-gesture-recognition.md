---
title: A Multi-Modal Framework with Cross-Subject Pseudo-Labeling and Semantic Alignment for Micro-Gesture Recognition
title_zh: 面向微手势识别的跨主体伪标签与语义对齐多模态框架
authors: "Haoran Zhang, Haokun Zhang, Pengyu Liu, Yujia Zhang, Weibao Xue, Yanbin Hao"
date: 2026-06-11
pdf: "https://arxiv.org/pdf/2606.13030v1"
tags: ["query:affective-ai"]
score: 9.0
evidence: 多模态微手势识别，捕捉身体动作传达的隐藏情感
tldr: "微手势识别面临极低信噪比、严重长尾分布及跨主题域偏移等挑战。本文提出显著性引导的多模态提取方法，集成骨架、3D热图和高分辨率RGB特征，并设计平方根平滑加权与正交语义嵌入损失平衡类别学习。引入跨模态伪标签策略实现无监督域适应，结合温度缩放软投票缓解融合过度自信。在MiGA-IJCAI挑战赛上取得68.13%的F1分数，排名第四，验证了框架在微手势识别中的有效性与泛化能力。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.13030v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1253, \"height\": 533, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13030v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1205, \"height\": 510, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13030v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 926, \"height\": 512, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.13030v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1274, \"height\": 808, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13030v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1197, \"height\": 373, \"label\": \"Table\"}]"
motivation: 微手势识别因极低信噪比、长尾分布和跨主题域偏移而极具挑战，亟需有效多模态方案。
method: 提出显著性引导的多模态特征提取（骨架、3D热图、高分辨率RGB），平方根平滑加权与正交语义嵌入损失，跨模态伪标签域适应及温度缩放软投票融合。
result: "在MiGA-IJCAI挑战赛Track 1上取得68.13%的F1分数，排名第四。"
conclusion: 多模态框架结合伪标签域适应有效提升微手势识别泛化能力，为挑战性跨主题识别提供新思路。
---

## 摘要
微手势是自发且细微的身体动作，常传递隐藏的人类情绪。在未裁剪视频中识别微手势极具挑战性，因其极低的信噪比、严重的类别长尾分布以及跨主体评估场景中固有的域偏移。本文为第四届MiGA-IJCAI挑战赛的第一赛道提出了一个综合多模态框架。为捕捉细粒度表征，我们设计了一个显著性引导的多模态提取流程，融合了68个关键点骨架关节坐标、三维热图体积和高分辨率RGB视觉特征。我们引入了一种平滑的平方根加权机制，结合正交语义嵌入损失，在保护尾部类别的同时不损害整体识别能力。更重要的是，为弥合跨主体泛化差距，我们提出了一种用于无监督域适应的跨模态伪标签策略，显著提升了单模态鲁棒性。最后利用温度缩放软投票机制来缓解后期融合中的过度自信。大量实验表明，我们的框架达到了68.13%的竞争性F1分数，获得第四名。

## Abstract
Micro-gestures (MGs) are spontaneous and subtle body movements that frequently convey hidden human emotions. Recognizing MGs in untrimmed videos remains highly challenging due to their extremely low signal-to-noise ratio, severe long-tailed class distribution, and the inherent domain shift encountered in cross-subject evaluation scenarios. In this paper, we propose a comprehensive multi-modal framework for Track 1 of the 4th MiGA-IJCAI Challenge. To capture fine-grained representations, we design a saliency-guided multi-modal extraction pipeline integrating 68-keypoint skeleton joint coordinates, 3D heatmap volumes, and high-resolution RGB visual features. We introduce a gentle square-root smoothed weighting mechanism paired with an Orthogonal Semantic Embedding Loss to protect tail classes without compromising overall recognition capabilities. More importantly, to bridge the cross-subject generalization gap, we propose a Cross-Modal Pseudo-Labeling (CMPL) strategy for unsupervised domain adaptation, which significantly boosts single-modal robustness. A temperature-scaled soft-voting mechanism is finally utilized to alleviate overconfidence during late fusion. Extensive experiments demonstrate that our framework achieves a competitive F1-score of 68.13\%, securing the 4th place.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **研究动机**：微手势（如揉鼻子、咬嘴唇等）是传递隐藏情绪的自发性细微身体动作，但其自动识别面临三大瓶颈：极低的信噪比（动作幅度小、与背景难以区分）、严重的类别长尾分布（稀有类别样本极度稀缺）以及跨主体评估时固有的域偏移（目标被试与训练被试的行为风格差异）。
- **核心问题**：如何在未裁剪视频中，设计一个鲁棒的多模态框架，同时克服上述三重挑战，实现高精度的跨主体微手势分类。
- **整体含义**：该工作服务于第四届 MiGA-IJCAI 挑战赛 Track 1，旨在推动情感计算与临床心理学领域对非言语行为的理解，其提出的跨模态伪标签策略和温度缩放融合等方法，为长尾、跨域细粒度行为识别提供了一种有效范式。

### 2. 方法论
- **整体架构**：构建一个融合骨架关节坐标、三维热图体积与高分辨率 RGB 的显著性引导多模态框架，并通过正交语义嵌入损失与跨模态伪标签域适应增强跨主体泛化。
- **关键技术与流程**：
  - **显著性引导的多模态编码**：
    - **骨架关节分支**：使用 OpenPose 提取 68 个关键点（含 19 个面部点），经中心归一化后输入解耦时空卷积网络（Decoupled ST-CNN），捕捉关节坐标序列表的细粒度空间位移。
    - **三维热图分支（PoseC3D）**：将 68 个关键点渲染为三维热图体积，利用 3D-ResNet50 进行时空连续建模，缓解坐标抖动与跟踪失败。
    - **显著性引导的视觉分支**：基于骨架坐标动态裁剪人物 ROI（保留 20% 安全边距），放大至 224×224 后送入 Video Swin Transformer 和 R(2+1)D 提取高分辨率 RGB 纹理特征。
  - **长尾分布校准**：
    - **平方根平滑加权**：为每个类别计算权重 \( w_c = 1⁄\sqrt{N_c + 1} \)，嵌入 Focal Loss，温和地提升稀有类别的关注度，避免梯度爆炸。
    - **正交语义嵌入损失**：在分类器前端强制特征 \( f \) 向一个随机初始化并经 QR 分解正交化的语义锚点 \( M_y \) 对齐，损失公式为 \( L_{sem} = \text{MSE}(f, M_y) = \frac{1}{d}\sum_{i=1}^{d}(f_i – M_{y,i})^2 \)，以此增大类间距离，防止尾部类被头部类“吞没”。
  - **跨模态伪标签策略**：
    - 利用基线多模态模型对无标签测试集生成预测，筛选最大 softmax 概率高于阈值（α = 0.65）的样本作为伪标签。
    - 将这些伪标签样本注入训练集/验证集，形成超级数据集 \( D_{aug} = D_{train} \cup D_{val} \cup D_{test}(\hat{Y}_{test}) \)，并从头重新训练模型，隐式对齐目标域分布，显著提升单模态鲁棒性。
  - **温度缩放多模态融合**：
    - 对每个模型的 logits \( L_m \) 先做温度缩放：\( P_m^{(c)} = \frac{\exp(L_m^{(c)}/\tau)}{\sum_{j=1}^{K}\exp(L_m^{(j)}/\tau)} \)，τ = 2.5。
    - 再进行加权求和：\( P_{final}^{(c)} = \sum_{m=1}^{M} W_m P_m^{(c)} \)，权重通过验证集网格搜索确定，缓解过度自信的“否决效应”。

### 3. 实验设计
- **数据集与场景**：iMiGUE 数据集，384 个视频，涉及 72 名被试的 32 种自发微手势，严格按被试划分训练、验证、测试集，无身份重叠。
- **评价指标**：Top‑1 准确率（等同于 Micro F1‑Score）。
- **对比方法**：
  - **往届最强方案**：MiGA’23–26 届各 Track 1 的前几名方法，涵盖 PoseConv3D、超图卷积 Transformer、VideoSwin 等，模态组合多样。
  - **基线方法**：35 关键点 ST‑GCN、未经裁剪的 R(2+1)D、高分辨率裁剪 R(2+1)D、PoseC3D 等。
  - **自身变体**：在消融实验中逐步添加 68 关键点、解耦 ST‑CNN、语义损失、跨模态伪标签，以及不同晚期融合策略（LightGBM 堆叠 vs. 温度缩放软投票）。

### 4. 资源与算力
- 文中**未明确说明**使用的 GPU 型号、数量或总训练时长。
- 仅给出部分优化细节：PoseC3D 使用 SGD 训练 100 个 epoch，每 GPU 批量大小为 64；RGB 分支采用 Kinetics‑400 预训练模型。由于缺乏硬件信息，无法评估其算力成本与可复现性，但考虑到多模态多条支路的并行训练与伪标签重训练，算力需求预估较高。

### 5. 实验数量与充分性
- **实验组数**：核心结果表 1 列举了自 2023 年至 2026 年共 10 个顶级提交方案的性能；消融表 2 包含 7 组对照（从基线到完整模型），并额外讨论了不同融合策略带来的差异。
- **充分性评价**：
  - 消融实验覆盖了数据增强（高分辨率裁剪、人脸关键点添加）、网络结构、长尾正则化、域适应和融合策略，链条完整。
  - 对比范围较广，涵盖了历年冠军方案，且均在同一测试服务器上公平评估，具备客观性。
  - 但实验仅局限于 iMiGUE 单一数据集，未在其他微动作或细粒度行为数据集上验证泛化能力；同时，消融未剥离头部分类与尾部分类的性能变化，难以量化对长尾问题的具体改善幅度。

### 6. 主要结论与发现
- 融合骨架、三维热图与高分辨率 RGB 的多模态编码可有效压制背景噪声，捕捉微手势的精细特征。
- 正交语义嵌入损失能通过固定的语义锚点增大类别间距，避免长尾分布下的模式坍塌。
- 跨模态伪标签策略是解决跨主体域偏移的关键：用高置信度伪标签重新训练，使单模态骨架模型准确率从 61.70% 大幅提升至 66.65%。
- 温度缩放软投票比堆叠分类器等可学习融合方法在少样本验证集上更稳定，能可靠地缓解“否决效应”。
- 最终集成模型在 iMiGUE 测试集上获得 68.13% 准确率，排名第四，验证了方案的有效性。

### 7. 优点
- **问题覆盖全面**：针对低信噪比、长尾分布、跨主体域偏移三大瓶颈逐一设计了对应模块。
- **多模态融合精细**：不仅使用了骨架和 RGB，还引入热图体积并采用显著性裁剪，消除无关背景。
- **领域适应创新**：跨模态伪标签（CMPL）属于无监督，无需目标域真实标签，工程友好且效果显著。
- **融合策略合理**：温度缩放有效平滑了概率分布，避免单模型过度自信，融合方式简单且鲁棒。
- **挑战赛验证**：在顶级学术竞赛中排名前列，与众多强方法直接对比，结果可信度高。

### 8. 不足与局限
- **数据集单一**：仅在 iMiGUE 上测试，未在其他微动作或长尾行为数据集（如 MA‑52、SMG）上证明迁移能力。
- **长尾效果未量化**：消融实验仅给出整体准确率，未展示每类或头尾类的性能细分，难以评估尾部类是否真正受益。
- **算力与效率分析缺失**：未提及 GPU 型号、训练用时、推理延迟，影响实用性判读。
- **骨架依赖**：性能高度依赖 OpenPose 提取的 68 点精度，在遮挡、多人等复杂场景下可能退化。
- **伪标签阈值敏感**：固定阈值 0.65 基于经验，可能对分布偏移敏感，缺乏理论支撑或自适应调整机制。
- **可学习融合的短板**：文中指出堆叠分类器因验证集过小而过拟合，但未探索正则化或少量样本下的元学习等解决方案。

（完）
