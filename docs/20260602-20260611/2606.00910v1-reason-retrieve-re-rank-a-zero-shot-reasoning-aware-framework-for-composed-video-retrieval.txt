Title: Reason, Retrieve, Re-rank: A Zero-Shot Reasoning-Aware Framework for Composed Video Retrieval

URL Source: https://arxiv.org/pdf/2606.00910v1

Published Time: Tue, 02 Jun 2026 01:04:51 GMT

Number of Pages: 4

Markdown Content:
# Reason, Retrieve, Re-rank: A Zero-Shot Reasoning-Aware Framework for Composed Video Retrieval 

## Ali Alavi The Ohio State University 

> alavibajestan.1@osu.edu

## Abstract 

Composed Video Retrieval (CoVR) seeks the target video that results from applying a free-form textual modification to a reference video. We address the Reason-Aware CoVR (CoVR-R) challenge at the CVPR 2026 VidLLMs workshop, where retrieval is strictly zero-shot. We present R3-CoVR (Reason, Retrieve, Re-rank ), a training-free pipeline built entirely from frozen foundation models. A multimodal large language model (Qwen3-VL-8B) reasons about the after-effects an edit implies—state transitions, action phases, scene, camera and tempo—and verbalises a concise post-edit description; a contrastive video–text encoder (SigLIP-2) embeds this description and the gallery for first-stage retrieval; finally a constraint-aware re-ranking stage uses the same multimodal model as a judge that scores each shortlisted candidate against the intended edited result. On the challenge test set, R3-CoVR attains 91.9% R@1 and 98.2% R@10 . Two findings drive these results: (i) matching the description length to the contrastive encoder’s text window lifts R@ 1from 67 .5 to 72 .7; and (ii) the constraint-aware re-ranker, which reorders only the shortlist, lifts R@ 1 from 72 .7 to 

91 .9—the single largest gain. We analyse the re-ranker’s behaviour, the retrieve/re-rank blend, and the shortlist depth, and we release a clean three-layer implementation. 

## 1. Introduction 

Composed Video Retrieval (CoVR) generalises text-to-video retrieval with a visual anchor: given a reference video Vr

and a modification instruction E, a system must retrieve, from a large gallery, the target video that reflects the re-quested compositional change while preserving the relevant context of Vr [ 2]. The task underpins content-based video recommendation, intelligent editing, and creative search. The CoVR-R challenge at the CVPR 2026 VidLLMs workshop sharpens the problem along two axes. First, re-trieval is zero-shot : models may not be trained or tuned on the challenge data, so the gallery must be matched by gener-alisation rather than memorisation. Second, the benchmark is reasoning-aware : edits are written so that their visual consequences are implied rather than stated. “Change typing to frustration” implies clenched fists and a closing laptop; “make it a close-up” implies tighter framing and shorter du-ration. Hard distractors that share surface vocabulary with the edit but violate its implied consequences defeat na ¨ıve keyword matching [1]. These constraints point away from trained fusion net-works and toward a verbalize-then-match strategy, as established for composed image retrieval by training-free, language-driven methods such as CIReVL [ 4] and CoTMR [ 5]. We adopt this view for video and push it further with an explicit re-ranking stage. Our system, R3-CoVR ,has three stages (Fig. 1): 1. Reason. A frozen multimodal LLM watches Vr and reasons about the after-effects of E as a structured trace, then writes a concise post-edit target description. 2. Retrieve. A frozen contrastive video–text encoder em-beds the description and every gallery video; cosine simi-larity yields a shortlist. 3. Re-rank. The multimodal LLM acts as a constraint judge, scoring each shortlisted candidate on how well it realises the intended result; scores are fused with the retrieval rank to reorder the head of the list. Our contributions are: (1) a fully zero-shot, training-free CoVR pipeline that attains 91 .9% R@1 on the challenge test set; (2) the observation that description length must match the contrastive text encoder’s token window , worth +5 .2

R@1; (3) a constraint-aware re-ranking stage that contributes 

+19 .2 R@1, with analysis of the retrieve/re-rank blend, the shortlist depth, and the judge’s score resolution; and (4) a reusable three-layer (data / model / runner) implementation. 

## 2. Related Work 

Composed retrieval. Composed image retrieval (CIR) fuses a reference image with a modification text and was popularised by CIRR and FashionIQ; CoVR extends the setting to video, with WebVid-CoVR providing large-scale 1            

> arXiv:2606.00910v1 [cs.CV] 30 May 2026 Stage 1: Reason
> Stage 2: Retrieve
> Stage 3: Re-rank
> Reference video Vr
> Edit text E
> Qwen3-VL (frozen) after-effect reasoning
> R={states, actions, scene, camera, tempo }
> concise post-edit description D
> SigLIP-2 text enc. →q
> Gallery videos {Vi}
> SigLIP-2 frames mean-pool →vi
> cosine sim. top-K
> shortlist
> Qwen3-VL judge constraint score sj
> (5-sample avg.)
> blend
> λs j+(1 −λ)rank
> final
> ranking
> shortlist

Figure 1. R3-CoVR. A frozen multimodal LLM reasons about the after-effects of the edit and writes a concise target description (Stage 1); a frozen contrastive encoder embeds the description and the gallery and returns a top-K shortlist (Stage 2); the multimodal LLM then judges each shortlisted candidate against the intended result, and the judge score is blended with the retrieval rank to reorder the head of the list (Stage 3). No component is trained on challenge data. 

weakly-supervised triplets [ 2]. Trained approaches learn a cross-attention fusion module, e.g. the enriched-context, discriminative-embedding line on Dense-WebVid-CoVR [ 3]. 

Training-free, language-driven retrieval. A complemen-tary direction replaces the learned fusion module with lan-guage. CIReVL [ 4] captions the reference, lets an LLM rewrite the caption under the modification, and retrieves with a frozen vision–language encoder; CoTMR [ 5] adds chain-of-thought, multi-scale reasoning. These methods show that language is a strong compositional interface and that training-free pipelines are competitive. The CoVR-R baseline [ 1] brings this to video with a frozen Qwen3-VL that reasons about edit consequences before matching. 

Re-ranking. Re-ranking a first-stage shortlist is standard in retrieval. In the training-free composed setting, constraint-based re-rankers such as Soft Filtering [ 6] verify prescriptive and proscriptive textual constraints to sharpen precision. R3-CoVR’s Stage 3 is a multimodal realisation of this idea: the judge inspects the candidate video and scores realisation of the edit, which is what most improves R@1. 

## 3. Method 

3.1. Problem setup 

Given a reference video Vr and a modification E, rank a gallery G = {V1, . . . , V N } so the ground-truth target V ⋆

appears as high as possible. We never train on challenge data; every model is frozen. 

3.2. Stage 1 – Reason 

A frozen Qwen3-VL-8B observes Vr (sampled at 1 fps) and is prompted to emit a structured after-effect trace 

R = {states , actions , scene , camera , tempo } followed by a target description D, in a single generation. Reasoning about consequences—rather than copying edit keywords—is what separates the target from hard distractors. To stabilise the description we draw K=3 samples (self-consistency) and average their embeddings downstream. The trace also serves as the human-legible reasoning trace required by the challenge. 

3.3. Stage 2 – Retrieve 

A frozen SigLIP-2 contrastive encoder maps text and video into a shared metric space. Each gallery video is encoded from 8 uniformly sampled frames, mean-pooled and L2-normalised; the query embedding q is the normalised mean of the description embeddings. Retrieval ranks G by co-sine similarity s(V ) = q⊤v(V ), excluding the query’s own reference, and returns a top-K shortlist. 

Description length matters. The contrastive text encoder has a fixed token window (64 tokens for SigLIP-2). A vivid, paragraph-length description is truncated, discarding salient post-edit content. Constraining D to a concise, front-loaded 

caption ( ≤ 40 words) that names the changed subjects, ob-jects and setting fits the window and is the single most ef-fective Stage-2 choice (§4.2). The rich reasoning trace is retained separately for the challenge’s reasoning metric. 

3.4. Stage 3 – Re-rank 

Stage 2 reliably places the target in the shortlist (R@10 ≈97% ) but not always at rank 1. Stage 3 reorders the shortlist with the same frozen Qwen3-VL acting as a 

constraint judge : shown a candidate video, the edit E, and the intended description D, it returns an integer match score in [0 , 100] , normalised to sj ∈ [0 , 1] . The final score blends 2the judge with the retrieval rank, 

sfinal (V ) = λ s j (V ) + (1 − λ) rank K (V ), (1) where rank K (V ) = ( K − i)/K for shortlist position i. The judge is discriminative—most distractors receive 0 while true matches cluster near 1—so the blend with the retrieval rank breaks ties and guards against the occasional false posi-tive. Re-ranking touches only the K shortlisted candidates, bounding cost. 

De-noising the judge. A greedy judge emits only a hand-ful of distinct scores, so a target and a distractor frequently tie at the maximum and the tiebreak decides rank 1. We average the score over 5 sampled generations, increasing the number of distinct score values by an order of magnitude (8 → 86 in our test pool) and resolving these ties. 

## 4. Experiments 

4.1. Dataset, metrics, implementation 

The CoVR-R challenge spans two galleries: WebVid (2,679 clips) and Something-Something-v2 (1,654 clips). The test split has 301 queries ( 4 WebVid, 297 SS2); each query is ranked against all videos in its gallery. We report Recall@ K

for K ∈ { 1, 5, 10 , 50 }, the official metric; the headline is R@1. Stage 1/3 use Qwen3-VL-8B-Instruct; Stage 2 uses 

siglip2-so400m-patch16-naflex . All models are frozen and run on a single A100 (40 GB). Gallery embed-dings are computed once and cached; the shortlist depth is 

K=10 unless noted, and the blend is λ=0 .8.

4.2. Main results 

Table 1 traces the contribution of each design choice on the challenge test set. First-stage retrieval with a rich description already clears the published reference point, but truncation caps recall. Matching the description to the encoder window (concise) lifts R@1 by +5 .2 and R@5 by a striking +16 .5.The constraint-aware re-ranker then lifts R@1 by a further 

+19 .2 to 91 .9. Extending the shortlist to K=20 trades a hair of R@1 for the best broad recall ( R@ 10 = 98 .2), as the judge promotes targets that first-stage retrieval buried below rank 10. For context, the published CoVR-R ref-erence reports 49 .9 R@1 on its own benchmark split [ 1]; our numbers are on the challenge test split and are not a direct head-to-head, but the gap indicates the strength of the pipeline. 

4.3. Ablations 

Retrieve/re-rank blend. Table 2 sweeps λ in Eq. (1) at 

K=10 . Pure retrieval ( λ=0 ) is the Stage-2 result ( 72 .7); pure judge ( λ=1 ) reaches 88 .2 but discards the useful re-trieval prior, while a judge-dominant blend ( λ ∈ [0 .5, 0.8] ) is best, peaking at 91 .9. The judge supplies most of the signal, but retaining a fraction of the retrieval rank guards against judge false positives. 

Shortlist depth. Table 3 contrasts K=10 and K=20 . A deeper shortlist gives the judge access to targets ranked 11 –

20 by Stage 2, improving R@5 and R@10 ( 96 .8 → 98 .2); R@1 is essentially unchanged, as deeper lists also expose more high-scoring distractors. Depth thus trades top-1 preci-sion for broad recall. 

Judge resolution. Replacing the greedy judge with the 

5-sample averaged judge raises the number of distinct can-didate scores from 8 to 86 in the test pool, removing the ties that otherwise force the retrieval-rank tiebreak to decide rank 1; this is the mechanism the blend in Table 2 partially compensates for. 

4.4. Implementation notes 

The system is organised in three layers— data , model , and 

runner —behind stable interfaces, with factory-instantiated encoders and reasoners, so a backbone can be swapped with-out touching the orchestration. Gallery embeddings are cached once per encoder; per-query reasoning and judge scores are cached and resumable. First-stage retrieval over both galleries and reasoning over all 301 queries complete in well under an hour on one A100; re-ranking the top-10 adds ∼2 s per query. 

4.5. Discussion and limitations 

Three properties make R3-CoVR practical. It is training-free : every component is a frozen, publicly available model, so there is no risk of over-fitting to the challenge data and the pipeline transfers directly to new galleries. It is inter-pretable : the Stage-1 trace and the Stage-3 judge rationale expose why a video is preferred, which the challenge also rewards through its reasoning-trace metric. And it is modu-lar : stronger encoders or judges drop in behind the factory interface. Several limitations point to future work. (i) The re-ranker’s cost grows linearly with the shortlist depth K and with the judge’s per-candidate latency; very large galleries would benefit from a cheaper first pass or a cascaded judge. (ii) The judge inherits the backbone’s perceptual biases; the residual gap between R@1 ( 91 .9) and the R@10 ceiling (96 .8–98 .2) is dominated by a handful of fine-grained pairs that an 8B judge scores identically, and a larger judge (e.g. a 

72 B variant) is the most direct way to close it. (iii) The con-trastive encoder’s 64 -token text window bounds how much of the reasoned description can inform first-stage retrieval; a long-context or video-native retriever would relax this and likely lift the shortlist recall that upper-bounds the re-ranker. (iv) Self-consistency and the de-noised judge trade compute 3Table 1. Main results on the CoVR-R challenge test set (301 queries). Each row adds one design choice over the row above. Re-ranking uses the Qwen3-VL constraint judge with blend λ=0 .8.Configuration R@1 R@5 R@10 R@50 (a) Reason + Retrieve, rich description 67.47 78.41 80.60 97.65 (b) Reason + Retrieve, concise description 72.69 94.95 96.80 99.16 (c) + constraint re-rank (top-10) 91.92 96.80 96.80 99.16 (d) + constraint re-rank (top-20) 91.58 97.81 98.15 99.16 

Table 2. Retrieve/re-rank blend λ (Eq. 1), top-10 shortlist, chal-lenge test R@1. λ=0 is pure retrieval, λ=1 is pure judge.               

> λ0.0 0.3 0.5 0.6 0.7 0.8 1.0 R@1 72.69 89.90 91.08 91.58 91.41 91.92 88.22

Table 3. Shortlist depth for the constraint re-ranker (blend λ=0 .7). Deeper shortlists favour broad recall. Depth R@1 R@5 R@10 R@50 top-10 91.41 96.80 96.80 99.16 top-20 91.58 97.81 98.15 99.16 

(3 × generation, 5 × scoring) for robustness; the trade-off is favourable here but should be revisited at scale. 

## 5. Conclusion 

R3-CoVR shows that reason-aware composed video retrieval is well served by a fully zero-shot, three-stage pipeline of frozen models: reason about the edit’s after-effects in lan-guage, retrieve with a contrastive encoder, and re-rank the shortlist with the same multimodal model used as a constraint judge. Two simple but consequential choices—fitting the description to the encoder’s token window, and re-ranking only the shortlist—take R@1 from 67 .5 to 91 .9 on the chal-lenge test set, with the re-ranker contributing the majority of the gain. The approach trains nothing, exposes interpretable reasoning traces, and is straightforward to reproduce. 

## References 

[1] O. Thawakar, D. Demidov, V. Potlapalli, et al. CoVR-R: Reason-Aware Composed Video Retrieval. CVPR (Findings) ,2026. arXiv:2603.20190. [2] L. Ventura, A. Yang, C. Schmid, G. Varol. CoVR: Learning Composed Video Retrieval from Web Video Captions. AAAI ,2024. [3] Composed Video Retrieval via Enriched Context and Discrim-inative Embeddings. CVPR , 2024. [4] S. Karthik, K. Roth, M. Mancini, Z. Akata. Vision-by-Language for Training-Free Compositional Image Retrieval. 

ICLR , 2024. [5] CoTMR: Chain-of-Thought Multi-Scale Reasoning for Training-Free Zero-Shot Composed Image Retrieval. arXiv:2502.20826, 2025. [6] Soft Filtering: Guiding Zero-shot Composed Image Retrieval with Prescriptive and Proscriptive Constraints. arXiv:2512.20781, 2025. 

4