---
title: Task-Vector Arithmetic for Emotional Expressivity Control in Language-Model-Based Text-to-Speech
title_zh: 基于语言模型的文本到语音中用于情感表现力控制的任务向量算术
authors: "Daniel Oliveira de Brito, Arnaldo Candido Junior"
date: 2026-06-03
pdf: "https://arxiv.org/pdf/2606.05367v1"
tags: ["query:affective-ai"]
score: 10.0
evidence: 通过x-向量质心运算控制语言模型TTS的情绪表达力
tldr: 语言模型驱动的文本到语音（LM-TTS）情感强度控制面临挑战，任务向量算术在模块化TTS中有效但未验证于大规模LM-TTS。本研究通过系统消融实验发现情感韵律主要承载于说话人嵌入（x-vector），并提出在x-vector空间进行质心算术的无训练方法。在跨说话人和跨语言（英到巴西葡语）场景中，该方法平均提升情绪余弦相似度0.29（英文）和0.09（巴西葡语），同时高保持身份（SECS≥0.88）和零WER。结果表明在说话人嵌入上操作可规避算术方法与词元TTS架构的兼容问题，为LM-TTS情感表现力控制提供了新途径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.05367v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1720, \"height\": 602, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05367v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 754, \"height\": 462, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05367v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 730, \"height\": 473, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05367v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 728, \"height\": 470, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05367v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1182, \"height\": 447, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05367v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 702, \"height\": 574, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.05367v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1380, \"height\": 412, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05367v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1002, \"height\": 568, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05367v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1002, \"height\": 568, \"label\": \"Table\"}]"
motivation: 探索任务向量算术在大规模语言模型TTS中实现跨说话人情感强度控制的可行性与机制。
method: 通过消融实验定位情感信息于x-vector，提出基于x-vector质心差的情感方向向量无训练推理控制方法。
result: 在英文和巴西葡语保留说话人上，emotion2vec余弦相似度分别提升0.29和0.09，同时保持高身份相似度和零WER。
conclusion: 在说话人嵌入空间进行算术操作可有效控制LM-TTS情感表现力，避免了与词元架构的兼容问题。
---

## 摘要
我们研究了任务向量算术——该方法在模块化文本到语音（TTS）中成功用于跨说话人情感强度控制——能否迁移到基于语言模型骨干网络和上下文学习的大规模TTS系统（LM-TTS）中。通过在Qwen3-TTS-12Hz-1.7B上对四个逐渐细化的操作数——通过LoRA微调的模型权重、连续编解码嵌入、离散编解码标记以及由与合成骨干网络联合训练的ECAPA-TDNN编码器产生的说话人嵌入（x-向量）——进行系统性消融研究，我们将情感韵律的主要载体定位为x-向量。基于这一发现，我们提出了一种基于x-向量空间质心算术的无训练方法：情感方向$τ= \mathbb{E}_i[x(s_i,\text{emo})] -\mathbb{E}_i[x(s_i,\text{neutral})]$应用于未见过的目标说话人，即$x_{\text{new}} = x(\text{target},\text{neutral}) + α\cdotτ$。以ESD（英语）作为$τ$源，emoUERJ（巴西葡萄牙语）作为跨语言真实目标，我们观察到在英语留出说话人上，相比ICL基线，emotion2vec余弦平均提升$+0.29$，巴西葡萄牙语留出说话人上提升$+0.09$，同时在很大程度上保持了身份信息（多说话人$τ$变体的WavLM SECS $\gtrsim 0.88$）和可懂度（PT-BR中WER $\approx 0$）。这些结果给出了初步证据，表明当算术操作在说话人嵌入上进行时，可以规避已报道的基于质心算术的风格控制与基于标记的TTS架构的不兼容性。

## Abstract
We investigate whether task-vector arithmetic, successful for cross-speaker emotional intensity control in modular text-to-speech (TTS), transfers to large-scale TTS systems built on language-model backbones with in-context learning (LM-TTS). Through a systematic elimination study over four progressively narrower operands on Qwen3-TTS-12Hz-1.7B - model weights via LoRA fine-tuning, continuous codec embeddings, discrete codec tokens, and the speaker embedding (x-vector) produced by an ECAPA-TDNN encoder jointly trained with the synthesis backbone - we localize the dominant carrier of emotional prosody to the x-vector. Building on this finding, we propose a training-free method based on centroid arithmetic in x-vector space: an emotion direction $τ= \mathbb{E}_i[x(s_i,\text{emo})] -\mathbb{E}_i[x(s_i,\text{neutral})]$ applied to an unseen target speaker as $x_{\text{new}} = x(\text{target},\text{neutral}) + α\cdotτ$. Using ESD (English) as the $τ$ source and emoUERJ (Brazilian Portuguese) as a cross-lingual ground-truth target, we observe average gains of $+0.29$ in emotion2vec cosine over the ICL baseline on English held-out speakers and $+0.09$ on Brazilian Portuguese held-out speakers, while largely preserving identity (WavLM SECS $\gtrsim 0.88$ for the multi-speaker $τ$ variant) and intelligibility (WER $\approx 0$ in PT-BR). These results offer initial evidence that the reported incompatibility of centroid-arithmetic style control with token-based TTS architectures may be circumvented when the arithmetic operates on the speaker embedding.

---

## 论文详细总结（自动生成）

好的，以下是根据论文《Task-Vector Arithmetic for Emotional Expressivity Control in Language-Model-Based Text-to-Speech》生成的结构化中文总结。

---

### 1. 论文的核心问题与整体含义

- **研究背景**：语音合成中对情感强度的控制（如“强烈愤怒”或“轻微悲伤”）对对话助手、有声书和自动配音等应用至关重要。近年来，基于任务向量算术（task-vector arithmetic）的方法在模块化TTS架构（如Conformer-FastSpeech2、VITS）上成功实现了跨说话人的情感传递与强度控制。
- **核心问题**：上述方法是否能迁移到当前主流的、基于语言模型骨干和离散语音token的语言模型TTS（LM-TTS）系统中？这类系统（如Qwen3-TTS）的情感控制通常依赖条件式自回归生成，缺乏明确的参数化情感模块，且已报道过该类架构与基于质心算术的风格控制不兼容。
- **整体含义**：本文试图回答“在LM-TTS架构中，情感信息实际驻留在哪个组件（权重、编码器嵌入、离散token还是说话人嵌入）”，并在此基础上探索一种训练免费、可迁移的跨说话人情感控制方法，为无需微调的LM-TTS情感表达力控制开辟新路径。

### 2. 论文提出的方法论

- **核心思想**：通过四个层次的系统性消融研究，逐步缩小操作数的范围，最终定位情感韵律的主要载体为说话人嵌入（x-vector），进而提出在x-vector空间进行质心算术的无训练情绪控制方法。
- **消融步骤与关键技术细节**：
  - **步骤1（权重空间）**：在全量微调或LoRA微调后计算任务向量 $\tau = \theta_{\text{ft}}^{\text{angry}} - \theta_{\text{ft}}^{\text{neutral}}$，并应用到Qwen3-TTS。结果仅生成噪声或平静语音，未产生可控的情绪输出。
  - **步骤2（编解码嵌入）**：计算每个码本（codebook）中愤怒与中性质心的差异向量 $\tau_k$，并以“一次求和”或“逐层独立扰动”方式注入连续嵌入。结果在发生作用与产生退行噪声之间急剧过渡，未产生可理解的中间情绪体制。
  - **步骤3（离散token替换）**：在相同文本的愤怒与中性样本对上，进行完全交换（愤怒token + 中性x-vector）。合成输出为平静、可理解的语音，表明LM忽略了token携带的情绪信息，而是依赖于x-vector。
  - **步骤4（x-向量空间）**：最终提出基于说话人嵌入质心差的方法。定义情感方向向量：
    $$\tau_{\text{emo}} = \mathbb{E}_{s\in S}[x(s, \text{emo})] - \mathbb{E}_{s\in S}[x(s, \text{neutral})]$$
    对未见过的目标说话人，以 $\alpha$ 控制强度：
    $$x_{\text{new}} = x(\text{target}, \text{neutral}) + \alpha \cdot \tau_{\text{emo}}$$
    多说话人平均变体（$\tau_{\text{avg4spk}}$）通过跨4名源说话人取平均来抵消掉源说话人的音色残留，仅保留共享的情感方向。

### 3. 实验设计

- **数据集与场景**：
  - **源数据**：英文情感语音数据库ESD，4名说话人（0011, 0014, 0017, 0020）用于提取多说话人$\tau$，其中0017单独用于单说话人$\tau$。
  - **英文留出目标**：ESD中未用于提取$\tau$的另2名说话人（0013, 0019），每人使用30句测试句，形成**同语种跨说话人**场景。
  - **跨语言目标**：巴西葡萄牙语数据emoUERJ，含3名说话人（m03, m04, w04），部分具有并行文本对，构成**英→巴西葡语跨语言、有真实参照**的验证场景。
- **对比基准与变体**：
  - **基线与系统**：`Base`为Qwen3-TTS纯上下文学习（$\alpha=0$），不施加任何情绪向量。对比变体包括`single0017`（单说话人$\tau$）和`avg4spk`（多说话人平均$\tau$）。
  - **真实天花板（Ceiling）**：同一说话人、相同文本的真实情感录音，提供指标上限参考。
- **评估指标**：
  - **EECS**：合成语音与真实情感语音的emotion2vec_plus_large余弦相似度，衡量情感传递。
  - **SECS_W**：与中性真实录音的WavLM说话人嵌入余弦相似度，衡量身份保持（采用独立于操作数的编码器）。
  - **WER**：Whisper-large-v3词错率，衡量可懂度。
  - **UTMOSv2**：自然度自动评估（跨语言时作为组内相对代理指标）。
  - **xvec_cos_GT**：跨语言实验中，合成x-vector与同一说话人真实情感录音x-vector的余弦相似度，直接验证算术假设。

### 4. 资源与算力

- 论文**未明确提及**所使用GPU的型号、数量及训练时长。仅提到在步骤1的全微调/LoRA实验中扫描了多个学习率和epoch，以及该方法核心为无需训练的推理阶段操作，因此计算诉求主要集中在单次嵌入平均和合成推理上，成本极低。

### 5. 实验数量与充分性

- **实验规模**：消融研究覆盖4组操作数，每组包含多种配置（如7个学习率×多个epoch、多个$\alpha$取值、8种离散token替换条件等）。
- **主实验**：在英文留出（2名说话人×3种情绪×2类$\tau$×7个$\alpha$值，每组30句话）和跨语言（3名说话人×3种情绪×2类$\tau$×5个$\alpha$值）两个设定下进行$\alpha$遍历。
- **充分性评价**：实验设计循序渐进，从权重到嵌入再到token和x-vector，具有清晰的因果消融逻辑；同语种和跨语言两套评估配合多指标，提供相对客观的定量依据。主要不足在于缺少感知层面的主观听力测试，以及方法验证仅局限于单一模型。

### 6. 论文的主要结论与发现

- **情感载体定位**：在Qwen3-TTS这类LM-TTS中，**情感韵律的主导载体是x-vector**（与合成骨干联合训练的ECAPA-TDNN说话人嵌入），而非骨干权重、编解码嵌入或离散token。
- **无训练控制可行性**：在x-vector空间进行多说话人质心算术，可在不进行任何微调的情况下，调控任意目标说话人的情绪表达强度。
- **性能增益**：英文留出说话人上，EECS相比上下文学习基线平均提升 **+0.29**，巴西葡语留出说话人上平均提升 **+0.09**。多说话人$\tau$（avg4spk）在保持身份相似度（SECS_W≥0.88）和可懂度（WER≈0）方面明显优于单说话人$\tau$。
- **不兼容性规避**：在说话人嵌入（而非专用风格嵌入）上进行质心运算，可绕过基于token的TTS架构对质心算术风格控制原本存在的不兼容问题。

### 7. 优点

- **清晰的因果消融**：通过离散token交换实验（愤怒token+中性x-vector→平静输出）直接证明了x-vector对情感的主导作用，证据链坚固。
- **训练免费与低消耗**：方法仅需要一次简单的x-vector平均，推理时通过单超参数$\alpha$即可实现实时情绪强度扫描，无需重训练，计算成本极低。
- **跨语言可迁移性**：使用英文情感方向向量成功迁移至巴西葡语发音人，并借助与真实录音匹配的xvec_cos_GT指标验证了方向的语言无关性和身份保持能力。
- **互补定位清晰**：与依赖昂贵微调或专用条件模块的同期工作（如IndexTTS2、MiniMax-Speech）形成对比，给出了一种轻量、可后验选择的替代思路。

### 8. 不足与局限

- **模型单一验证**：仅在Qwen3-TTS-12Hz-1.7B上测试，虽提出可适用于任意具有联合训练可学习说话人编码器的ICL-TTS，但泛化性缺乏实证。
- **编码器依赖性**：方法有效的前提是说话人编码器能捕获情感韵律的近似线性方向。若编码器专门用于说话人验证或施加了身份-情感解耦损失，$\alpha$的有效窗口可能显著缩窄甚至消失。
- **跨语言增益有限**：巴西葡语上的增益（+0.09）明显低于英语（+0.29），部分原因在于基线已较高（中性发音本身带情绪色彩）、emotion2vec在非英语上的灵敏度不足；缺乏母语感知验证。
- **微调负结果的条件性**：步骤1中权重算术的失败仅基于“单说话人/单情绪”的小数据量微调，不排除多说话人、多情绪大规模微调能出现操作窗口。
- **客观指标的饱和风险**：EECS在高$\alpha$区域可能因过度投影而饱和，而xvec_cos_GT因ECAPA对身份维度的高度压缩而自身不灵敏，这使$\alpha$最优值的选取缺乏可靠参照，亟需主观听力测试补充。
- **未涉及情感混合**：未探索多个情感$\tau$的线性组合以实现复合情绪，文中虽提及作为未来工作，但当前方法仅覆盖单一情绪类别。

（完）
