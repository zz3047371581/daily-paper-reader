---
title: Speech Emotion Recognition using Attention-based LSTM-Network with Residual Connection
title_zh: 基于注意力机制和残差连接的LSTM网络用于语音情感识别
authors: "Daniil Krasnoproshin, Maxim Vashkevich"
date: 2026-06-02
pdf: "https://arxiv.org/pdf/2606.03359v1"
tags: ["query:affective-ai"]
score: 10.0
evidence: 提出轻量级注意力LSTM加残差连接用于语音情感识别，是情感计算核心任务
tldr: 语音情感识别是构建自然交互界面的关键技术，然而当前性能领先的模型通常基于大规模预训练网络，计算开销大、内存占用高，难以在移动设备或实时系统中有效运行。针对这一挑战，本文设计了轻量级架构ResLSTM-SA，通过将残差连接与软注意力机制引入LSTM，增强时序建模能力且控制模型尺寸。在RAVDESS数据集上，采用说话人独立协议评估，ResLSTM-SA-h64仅需46.8k可训练参数，即取得0.6517的未加权平均召回率，优于传统注意力LSTM及若干CNN和CNN-LSTM混合模型，而参数量仅为大型自监督模型的千分之一。该工作证实，通过精心设计的轻量化结构，可在不牺牲过多精度的前提下实现高效情感推断，为边缘计算场景下的语音助手等提供可行方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.03359v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1705, \"height\": 605, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03359v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 699, \"height\": 837, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03359v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 617, \"height\": 1519, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03359v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 889, \"height\": 742, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03359v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1799, \"height\": 557, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.03359v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 878, \"height\": 263, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.03359v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 852, \"height\": 321, \"label\": \"Table\"}]"
motivation: 前沿方法多使用大型预训练模型，计算和内存开销巨大，难以在资源受限边缘设备上实时运行。
method: 提出ResLSTM-SA，融合残差连接与软注意力的轻量级LSTM网络，用于语音情感识别。
result: 在RAVDESS数据集说话人独立协议下，最佳变体以46.8k参数取得0.6517 UAR，优于传统方法，参数少三个数量级。
conclusion: 轻量化模型实现竞争性精度与极低计算成本，为实时语音情感识别提供可行方案，代码公开。
---

## 摘要
语音情感识别是现代人机交互系统的重要组成部分。然而，许多最先进的方法依赖于具有高计算和内存需求的大型预训练模型，限制了它们的应用范围。本文提出ResLSTM-SA，一种在LSTM框架内融合残差连接与软注意力的轻量级架构。在严格的说话人独立划分下，于RAVDESS数据集上进行评估，所提模型在未加权平均召回率（UAR）方面优于传统的基于注意力的LSTM基线以及先前报道的几种CNN和混合CNN-LSTM架构。最佳变体（ResLSTM-SA-h64）仅用46.8k可训练参数便实现了最高0.6517的UAR，以比大规模自监督替代方案少三个数量级的参数提供了有竞争力的精度，从而能够在边缘设备和实时语音助手上高效部署。源代码可在https://github.com/Mak-Sim/ResLSTM-SER获取。

## Abstract
Speech emotion recognition is an important component of modern human-computer interaction systems. However, many state-of-the-art approaches rely on large pretrained models with high computational and memory requirements, limiting their applicability. This paper proposes ResLSTM-SA, a lightweight architecture that integrates residual connections with soft attention within an LSTM-based framework. Evaluated on the RAVDESS dataset under strict speaker-independent partitioning, the proposed model outperforms conventional attention-based LSTM baselines and several previously reported CNN- and hybrid CNN-LSTM architectures in terms of unweighted average recall (UAR). The best-performing variant (ResLSTM-SA-h64) achieves a maximum UAR of 0.6517 with only 46.8k trainable parameters, delivering competitive accuracy with three orders of magnitude fewer parameters than large-scale self-supervised alternatives, thereby enabling efficient deployment on edge devices and real-time voice assistants. The source code is available at https://github.com/Mak-Sim/ResLSTM-SER.

---

## 论文详细总结（自动生成）

### 研究背景与核心问题
语音情感识别（SER）是构建自然、智能人机交互的关键技术，被广泛用于客服、健康监测、情感学习等场景。近年来，借助大规模预训练模型（如PANNs、wav2vec 2.0等），SER 的准确率已大幅提升。然而，这些先进模型通常包含数千万甚至上亿的可训练参数，推理时计算开销大、内存占用高，难以部署到资源受限的边缘设备或实时语音助手中。因此，如何在保持一定识别精度的前提下，设计出轻量、高效且能独立端到端训练的模型，成为该领域的一个突出矛盾。本文正是围绕“精度–效率权衡”这一核心问题，提出一种融合残差连接与软注意力的轻量级LSTM架构，力求以极少的参数实现有竞争力的识别性能。

### 方法论
论文提出了一种名为 **ResLSTM‑SA** 的轻量级语音情感识别模型，其核心思想是在经典的基于注意力的LSTM（LSTM‑SA）基础上，引入残差连接来增强时序表征能力，同时控制模型规模。

- **特征提取**：对原始语音逐帧提取 34 维梅尔频率倒谱系数（MFCC）和 12 维色度特征（chromagram），拼接为 46 维的联合特征向量。一段语音被转化为特征矩阵 \( X \in \mathbb{R}^{T \times 46} \)，其中 \( T \) 为帧数。
- **模型结构**：
  1. **第一个LSTM层**（残差层）：隐藏状态维度固定为 46（与输入特征维度一致），输出隐藏状态序列。
  2. **残差连接**：将该层输出与原始输入进行逐元素相加，形成上下文增强后的特征序列。这一步保证了原始声学信息不丢失，同时为后续层提供更丰富的时序上下文。
  3. **第二个LSTM层**（注意力层）：处理增强后的序列，输出隐藏状态序列 \( h_t \)（维度可配置，如 32、64、128）。
  4. **软注意力机制**：学习一个可训练的注意力向量 \( u \)，对第二层LSTM的隐藏状态进行加权求和，得到固定的上下文向量 \( h_{\text{context}} \)：
     \[
     e_t = u^T h_t,\quad \alpha_t = \text{softmax}(e_t),\quad h_{\text{context}} = \sum_{t=0}^{T-1} \alpha_t h_t.
     \]
  5. **分类器**：上下文向量通过全连接层和 softmax，输出 8 类情感概率。
- **训练设定**：损失函数为类别交叉熵；优化器使用 Adam，并配合余弦退火学习率调度；所有权重采用 Xavier 正态初始化；遗忘门偏置初始化为 1 以促进长期记忆。

模型变体记作 ResLSTM‑SA‑h`Y`，其中 `Y` 为第二层 LSTM 的隐藏单元数（32、64、128）。

### 实验设计
- **数据集**：RAVDESS 语音情感子集（1440 条语音，24 位专业演员，8 类情感，部分情感含两种强度）。  
- **评估协议**：采用说话人独立的 5 折交叉验证（与文献[3]提议的划分方案一致），确保训练集与验证集无相同说话人，避免身份泄漏，并便于与已有工作直接对比。  
- **评价指标**：未加权平均召回率（UAR），即对每个类别的召回率求算术平均，以平衡各类别样本量差异。  
- **对比方法**：
  - 基线：不同隐藏维度的 LSTM‑SA（无残差连接）。
  - 已发表架构：AlexNet embeddings + SVM、CNN+LSTM、GResNet+S、Fine‑tuned AlexNet、Fine‑tuned CNN14、Fine‑tuned xlsr‑wav2vec 2.0、wav2vec 2.0 + 数据增强。
- **超参数优化**：使用 Optuna 框架的 TPE（Tree‑Structured Parzen Estimator）算法，在以下搜索空间内为每个模型寻找最优配置：学习率 \([3\times10^{-5}, 2\times10^{-4}]\)、权重衰减 \([2\times10^{-5}, 2\times10^{-2}]\)、全连接层 dropout \([0.1, 0.5]\)、余弦退火周期 \(T_0 \in \{1,2,3,5,10\}\)、批大小 \(\{8,16,32,64\}\)。  
- **结果统计**：每一最优超参组合下，用不同随机种子独立训练 10 次，报告 UAR 的均值与标准差，以排除随机初始化的影响。

### 资源与算力
论文**未明确给出**所使用 GPU 型号、数量、单次训练时长或总训练耗时。文中仅提及所有模型均训练 100 个 epoch，且采用早停判断（验证集准确率在 100 epoch 后几乎不再提升）。鉴于模型参数量极小（最大仅 108.9k），训练应完全能在单 GPU 甚至 CPU 上短时间内完成，但具体算力资源仍缺乏量化说明。

### 实验数量与充分性
- **内部对比实验**：涵盖 2 种架构（LSTM‑SA 和 ResLSTM‑SA） × 3 种隐藏维度（32、64、128），共 6 组模型。每组经 Optuna 调参后，再进行 10 次随机种子测试，合计至少 60 次独立训练。  
- **外部对比**：引用了 6 种已发表方法在相同数据集上的结果，均采用说话人独立协议，增加了可比性。  
- **定性分析**：提供了最优模型的混淆矩阵、PCA 嵌入可视化，并展示了残差层激活与注意力权重的时序图，从多角度解释了模型行为。  
- **充分性评估**：实验在单一数据集（RAVDESS）上系统且严格，能够充分证明所提方法在该数据集上的优势与内部设计的有效性。然而，论文缺少在更多语料库（如 IEMOCAP、MELD 等）上的跨库验证，也未进行抗噪或跨语言测试，泛化性方面的证据不够充分。

### 主要结论与发现
1. **残差连接带来一致且显著的提升**：在所有对比的隐藏维度下，ResLSTM‑SA 的 UAR 均大幅超越 LSTM‑SA 基线，提升幅度最高达 7.7 个百分点。  
2. **参数效率极高**：最佳模型 ResLSTM‑SA‑h64 仅含 46.8k 可训练参数，UAR 均值 0.6232、最高值 0.6517，以约为 LSTM‑SA‑h128 一半的参数量，实现了更高精度；比大型自监督模型（如 317M 参数的 wav2vec 2.0）参数量少三个数量级，而精度差距相对可控。  
3. **容量与过拟合的平衡**：继续增大隐藏维度至 h=128 时，UAR 均值反降，表明所提架构在小尺寸下已能有效建模，过度增大会导致过拟合。  
4. **嵌入空间质量**：PCA 可视化显示，ResLSTM‑SA 提取的句子级表征形成了更加紧凑、类间分离清晰的聚类，而原始声学特征则高度重叠。  
5. **混淆特性**：混淆矩阵揭示“高兴”与“中性”之间存在较为严重的互混淆，这与其它的 SER 研究一致，提示情感表达的声学模糊性是普遍挑战。

### 优点
- **轻量高效**：以极少参数实现有竞争力的 UAR，适合边缘部署，并在推理速度和内存占用方面具有天然优势。  
- **设计简洁且有效**：仅通过一个维度匹配的残差 LSTM 层和软注意力，无需引入卷积、Transformer 或复杂预训练，便显著提升了 LSTM 的时序建模能力。  
- **实验严谨**：采用说话人独立协议、贝叶斯超参优化、多次种子试验取统计结果，避免了信息泄漏和调参偏见，对比公平性强。  
- **可复现性**：代码开源，实验细节（优化器、初始化、调度器等）描述清晰。

### 不足与局限
- **数据集单一**：仅在 RAVDESS 上评估，缺乏对其他标准 SER 数据库（如 IEMOCAP、Emo‑DB、MELD）的验证，跨语料泛化能力未知。  
- **外部对比的公平性受限**：对照方法的结果直接取自文献，可能数据预处理、划分细节或后处理不完全一致，未全部在同一实验条件下重新训练。  
- **未做全面的消融研究**：未单独剥离残差连接、注意力机制或特征组合（如仅用 MFCC 或 chroma）的作用，也缺少对注意力头数、连接方式等更细粒度的分析。  
- **无噪声/真实环境测试**：所有实验基于干净录音，未讨论在噪声、混响等实际声学环境下的鲁棒性。  
- **缺少计算效率指标**：未报告模型的实际推理时间、FLOPs 或内存占用，难以量化实时部署时的具体收益。  
- **语言与情感类别局限**：仅针对英语发音的 8 类情感，未讨论多语言或更多类别情感的可扩展性。

（完）
