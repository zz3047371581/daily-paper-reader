---
title: "Beyond Generative Decoding: Discriminative Hidden-State Readout from a Native Omni-Modal LLM for Multimodal Sentiment Analysis"
title_zh: 超越生成式解码：原生全模态大语言模型的判别式隐藏状态读出用于多模态情感分析
authors: "Bin Wen, Tien-Ping Tan"
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.05713v1"
tags: ["query:affective-ai"]
score: 9.0
evidence: 利用原生全模态LLM从语言、声学和视觉信号进行多模态情感分析
tldr: 多模态情感分析中，生成式读出通过离散解码输出连续分数，引入误差与延迟。本文提出基于原生全模态LLM（Qwen2.5-Omni-7B）的判别式读出，直接利用末层隐藏状态经回归头预测，采用QLoRA单GPU高效微调。在CMU-MOSI与CMU-MOSEI上达到领先精度，MAE显著低于生成式方法且多种子稳定。证明读出机制与训练同等关键，为连续情感分析提供更准确高效的方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.05713v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1526, \"height\": 683, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05713v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1516, \"height\": 747, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05713v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1666, \"height\": 411, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05713v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1545, \"height\": 680, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.05713v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1123, \"height\": 781, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05713v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1123, \"height\": 649, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05713v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1351, \"height\": 347, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05713v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1021, \"height\": 602, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05713v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1026, \"height\": 242, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05713v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 752, \"height\": 200, \"label\": \"Table\"}]"
motivation: 生成式读出依赖自回归文本解码，导致高误差、输出不可解析及延迟，需探索更适配连续回归的读出方式。
method: 采用Qwen2.5-Omni-7B的Thinker模块，提取末层非填充token隐藏状态，经轻量回归头直接输出情感分数，结合QLoRA在单GPU上训练。
result: "MOSI上MAE 0.551、Corr 0.888，MOSEI上MAE 0.506、Corr 0.790；生成式方法MAE加倍且有2.8%零样本输出不可解析。"
conclusion: 判别式读出避免离散解码缺陷，更准确、高效、稳定，凸显了LLM读出方式对连续多模态情感分析的重要性。
---

## 摘要
多模态情感分析（MSA）从语言、声学和视觉信号中推断人类情感。近年来的方法越来越多地通过生成式读出适配大型多模态模型（LMMs）：提示模型以文本字符串的形式输出情感分数。这种做法虽然方便，却将连续回归与离散自回归解码绑定在一起，带来了未测度的代价。我们重新审视这种读出机制，并基于原生全模态大语言模型（Qwen2.5-Omni-7B）的Thinker模块提出了一种判别式方案。我们不再进行文本解码，而是在单次前向传播中，通过一个轻量级回归头将最后一个非填充token的最终层隐藏状态映射为连续分数。使用4位量化和低秩适配（QLoRA），整个70亿参数的处理流程——包括视频和音频处理——可以在单张消费级GPU（RTX 5090，32 GB）上训练，峰值显存占用10-21 GB，可训练参数仅占1.14%。通过在固定骨干网络、数据和LoRA配置的情况下进行对照比较，我们分离出了读出方式带来的影响。在CMU-MOSI和CMU-MOSEI数据集上，我们的判别式读出无需任务特定的特征工程，即达到了最高准确率（MOSI：MAE 0.551，相关系数0.888；MOSEI：MAE 0.506，相关系数0.790），并且展现出很强的多随机种子稳定性。相比之下，生成式读出——即使经过同等的监督训练——也使平均绝对误差翻倍以上，产生无法解析或超出范围的输出（零样本时占2.8%），并且延迟更高。模态消融实验揭示出CMU-MOSI上文本主导的模式。我们的发现表明，如何读出大语言模型与其如何训练同等重要，证明了判别式读出为连续型多模态情感分析提供了一种更准确、高效且可靠的替代方案。

## Abstract
Multimodal sentiment analysis (MSA) infers human affect from language, acoustic, and visual signals. Recent methods increasingly adapt large multimodal models (LMMs) via generative readout: prompting the model to emit a sentiment score as a text string. While convenient, this ties continuous regression to discrete autoregressive decoding, incurring unmeasured costs. We revisit this readout mechanism and propose a discriminative formulation built on the Thinker module of a native omni-modal LLM (Qwen2.5-Omni-7B). Instead of text decoding, we map the final-layer hidden state of the last non-padding token to a continuous score via a lightweight regression head in a single forward pass. Using 4-bit quantization and low-rank adaptation (QLoRA), the entire 7B pipeline -- including video and audio processing -- trains on a single consumer GPU (RTX 5090, 32 GB) with 10-21 GB peak memory and 1.14% trainable parameters. Through a controlled comparison fixing the backbone, data, and LoRA configuration, we isolate the impact of the readout. On CMU-MOSI and CMU-MOSEI, our discriminative readout reaches state-of-the-art accuracy without task-specific feature engineering (MOSI: MAE 0.551, Corr 0.888; MOSEI: MAE 0.506, Corr 0.790) and exhibits strong multi-seed stability. In contrast, the generative readout -- even after equivalent supervised training -- more than doubles the mean absolute error, yields unparsable or out-of-range outputs (2.8% zero-shot), and suffers from higher latency. Modality ablations reveal a text-dominant regime on CMU-MOSI. Our findings indicate that how an LMM is read out is as consequential as how it is trained, demonstrating that a discriminative readout offers a more accurate, efficient, and reliable alternative for continuous MSA.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

- **研究背景**：多模态情感分析（MSA）从文本、语音、视觉中推断连续情感强度（如 [-3, +3]）。当前主流方法将大型多模态模型（LMM）作为文本生成器使用，即**生成式读出**：通过 prompt 让模型自回归输出一个数值字符串，再解析成数字。  
- **核心问题**：这种生成式读出将**连续回归任务强行耦合到离散的自回归解码通道**，可能带来精度天花板、推理延迟高、输出不可解析或超出范围等风险，但这些代价此前几乎未被定量测量。  
- **整体含义**：论文重新审视 LMM 用于 MSA 时的**读出机制**，提出并验证：**读出方式与模型训练同等重要**；对于连续情感分析，判别式读出比生成式读出更准确、高效且可靠。

### 2. 方法论

#### 2.1 核心思想
- 不依赖自回归生成，改为**从骨干模型的隐藏状态中直接回归连续情感分数**。  
- 利用原生全模态 LLM（Qwen2.5-Omni-7B）的 **Thinker 模块** 作为共享编码器，丢弃其生成语音的 Talker 头。  
- 仅使用**最后一个非填充 token 的最终层隐藏状态**，通过轻量级 MLP 头进行单次前向传播预测。

#### 2.2 关键技术细节与流程
- **输入处理**：文本转录本、均匀采样的视频帧（最多 16 帧，像素裁剪限定）、可选音频（经 DeepFilterNet 降噪）。  
- **编码**：模态专用编码器将各模态转为 token，拼接后送入 Thinker（Transformer 解码器）得到隐藏状态序列 \(\mathbf{H} = [\mathbf{h}_1, \dots, \mathbf{h}_T]\)，维度 \(d = 3584\)。  
- **池化**：利用注意力掩码定位最后一个非填充位置 \(\ell\)，取 \(\mathbf{z} = \mathbf{h}_{\ell}\) 作为汇总表示。  
- **回归头**：一个两层 MLP（Linear → LayerNorm → ReLU → Dropout → Linear），将 \(\mathbf{z}\) 映射为标量 \(\hat{y} \in [-3, +3]\)。  
- **训练目标**：先对标签进行训练集标准化（减去均值除标准差），用 \(L_1\) 损失（MAE）训练，输出时再逆标准化。  
- **参数高效适配**：  
  - 骨干网络采用 4-bit NF4 量化 + QLoRA（rank=32，注入注意力与 FFN 投影层），冻结基座参数，仅训练 LoRA 适配器和回归头，可训练参数约 1.14%（约 1.03 亿）。  
  - 优化器：AdamW，分层学习率（LoRA 部分 \(2\times10^{-4}\)，头部分 \(1\times10^{-3}\)），余弦衰减，梯度累计。  
- **生成式对照**：在完全相同的骨干、数据和 LoRA 配置下，将标签渲染为数字字符串，用语言建模交叉熵训练，推理时贪婪解码并正则解析，统计不可解析率和超出范围率。

### 3. 实验设计

#### 3.1 数据集与评估指标
- **数据集**：CMU-MOSI（2199 段独白）和 CMU-MOSEI（约 2.3 万段），均采用标准说话人独立划分。  
- **指标**：回归指标 MAE 和 Pearson 相关系数；分类指标七类准确率（Acc-7）、二类准确率（Acc-2）、加权 F1（均按非零极性惯例）。生成式读出额外报告不可解析率和超出范围率。

#### 3.2 对比方法
- **跨代方法**：包括 TFN、LMF、MFN、MulT、MISA、MAG-BERT、Self-MM、MMIM、ConFEDE、DMD、ALMT、2025 年的 MSAmba、MEMMI、DecAlign 等。  
- **核心控制对比**：在固定骨干、数据、LoRA 的条件下，比较**判别式读出** vs **生成式读出（零样本与训练后）**。  
- **消融实验**：  
  - 模态消融：文本 vs 文本+视频 vs 全模态。  
  - 音频降噪消融：原始音频 vs DeepFilterNet 降噪。  
- **稳定性检验**：CMU-MOSI 上 4 个随机种子。  
- **表示空间分析**：对隐藏状态进行 PCA/t-SNE 可视化，并计算 kNN 标签平滑度统计量。

### 4. 资源与算力

- **硬件**：所有实验在一张 **NVIDIA RTX 5090（32 GB）** 消费级 GPU 上完成。  
- **显存占用**：峰值 10–21 GB（使用 4-bit 量化、梯度检查点、无 KV 缓存训练）。  
- **训练时长**：论文未明确给出具体的训练小时数，但提到使用了梯度累积与生产者线程预取，在“实际时间预算内”完成。

### 5. 实验数量与充分性

- **实验数量**：约 6 张表格、4 幅图示，覆盖两种数据集、多种读出的定量对比、多个基线模型、消融与稳定性分析。  
- **充分性与公平性**：  
  - 核心创新点通过**严格控制变量**（相同骨干、数据、LoRA 配置）将读出机制的影响单离出来，公平且具有说服力。  
  - 包含多种子测试，说明结果非偶然。  
  - 消融设计解决了模态贡献与预处理影响。  
- **局限说明**：作者坦诚指出，CMU-MOSEI 上未运行多随机种子验证，模态消融与音频降噪消融仅在 MOSI 上完成，与经典方法的对比数据来自不同来源，存在评估偏差风险。

### 6. 论文的主要结论与发现

- **判别式读出全面优于生成式读出**：在完全相同主干与数据条件下，判别式方法大幅降低 MAE（MOSI 上从 ≥1.44 降至 0.667），提升相关性和稳定性，且**训练后生成式读出反而相关系数下降**（0.491→0.197），揭示了生成式目标与连续回归的根本性不匹配。  
- **高可靠性**：判别式头天然保证输出在数值范围内且可解析，而生成式读出的零样本不可解析率达 2.8%，即使训练后也存在超出范围输出。  
- **推理效率**：单一前向传播比自回归解码更快（单样本时间 1.14 s vs 1.47 s），峰值内存相当。  
- **表示空间已有情感结构**：Thinker 的隐藏状态经 PCA/t-SNE 呈现从负到正的平滑梯度，kNN 标签平滑度检验证实近邻标签高度一致，解释了轻量回归头即可胜任的原因。  
- **模态贡献**：CMU-MOSI 为文本主导型任务，加入视觉/音频对回归指标略有负影响，但作者公开讨论而非夸大。  
- **整体结论**：**读出方式与训练方式同样决定模型效果**；判别式隐藏状态读出是连续多模态情感分析更准确、高效、可靠的选择。

### 7. 优点

- **首次系统量化生成式读出的成本**，提供了严格的控制实验证明其劣势。  
- 在原生全模态 7B LLM 上实现**极简但高效的判别式回归**，无需手工特征提取与专用融合网络，且匹配 SOTA。  
- **低成本复现**：通过 QLoRA 将全模态训练压缩至单块消费级 GPU，大幅降低硬件门槛。  
- 实验设计严谨：对比、消融、多种子、表示分析层次分明；对数据集限制和文本主导现象作开诚布公的讨论。  
- 提供表示空间的可视化证据，增强了方法合理性的解释力。

### 8. 不足与局限

- **稳定性验证范围有限**：多种子测试只在 CMU-MOSI 上进行，CMU-MOSEI 因计算成本未做，该结果的一般性有待进一步证实。  
- **消融实验局限**：模态消融与音频降噪消融仅覆盖 MOSI，MOSEI 上缺少对应分析。  
- **基线对比公平性**：与早期方法的原始数字来自不同来源，其底层特征提取器和评估细则可能不完全一致，未来需在统一框架下重新评测。  
- **任务与语言泛化性**：工作仅针对英语连续情感回归，对于分类式情感识别、多语言场景是否同样适用尚未验证。  
- **训练时长未量化**：论文未提供具体训练时间成本数据，限制了实际部署的成本估算。  
- **依赖特定模型**：方法基于 Qwen2.5-Omni 的 Thinker 架构，迁移到其他原生全模态模型时的效果有待检验。

（完）
